/*
 * Copyright 2026 The OpenSSL Project Authors. All Rights Reserved.
 *
 * Licensed under the Apache License 2.0 (the "License").  You may not use
 * this file except in compliance with the License.  You can obtain a copy
 * in the file LICENSE in the source distribution or at
 * https://www.openssl.org/source/license.html
 */

#ifndef OSSL_CRYPTO_EC_PARAMS_H
#define OSSL_CRYPTO_EC_PARAMS_H

/* clang-format off */



#include "crypto/ec.h"
#include <openssl/core_names.h>
#include <openssl/err.h>
#include <openssl/proverr.h>



/* Selection-specific provider import and export declarations. */
#define ec_imexport_types_1_st ec_params_st
/* Machine generated by util/perl/OpenSSL/paramnames.pm */
#include <string.h>
#include <openssl/params.h>
#include <openssl/e_os2.h>
#include "internal/common.h"
#include "prov/proverr.h"

/* Machine generated */
#ifndef ec_imexport_types_1_list
static ossl_unused const OSSL_PARAM ec_imexport_types_1_list[] = {
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_PRIV_KEY, NULL, 0),
    OSSL_PARAM_END
};
#endif

#ifndef ec_imexport_types_1_st
struct ec_imexport_types_1_st {
    OSSL_PARAM *priv;
};
#endif

#ifndef ec_imexport_types_1_decoder
static ossl_unused int ec_imexport_types_1_decoder
    (const OSSL_PARAM *p, struct ec_imexport_types_1_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            if (ossl_likely(strcmp("priv", s + 0) == 0)) {
                /* OSSL_PKEY_PARAM_PRIV_KEY */
                if (ossl_unlikely(r->priv != NULL)) {
                    ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                   "param %s is repeated", s);
                    return 0;
                }
                r->priv = (OSSL_PARAM *)p;
            }
    return 1;
}
#endif
/* End of machine generated */
#define ec_imexport_types_2_st ec_params_st
/* Machine generated */
#ifndef ec_imexport_types_2_list
static ossl_unused const OSSL_PARAM ec_imexport_types_2_list[] = {
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_PUB_KEY, NULL, 0),
    OSSL_PARAM_END
};
#endif

#ifndef ec_imexport_types_2_st
struct ec_imexport_types_2_st {
    OSSL_PARAM *pub;
};
#endif

#ifndef ec_imexport_types_2_decoder
static ossl_unused int ec_imexport_types_2_decoder
    (const OSSL_PARAM *p, struct ec_imexport_types_2_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            if (ossl_likely(strcmp("pub", s + 0) == 0)) {
                /* OSSL_PKEY_PARAM_PUB_KEY */
                if (ossl_unlikely(r->pub != NULL)) {
                    ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                   "param %s is repeated", s);
                    return 0;
                }
                r->pub = (OSSL_PARAM *)p;
            }
    return 1;
}
#endif
/* End of machine generated */
#define ec_imexport_types_3_st ec_params_st
/* Machine generated */
#ifndef ec_imexport_types_3_list
static ossl_unused const OSSL_PARAM ec_imexport_types_3_list[] = {
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_PRIV_KEY, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_PUB_KEY, NULL, 0),
    OSSL_PARAM_END
};
#endif

#ifndef ec_imexport_types_3_st
struct ec_imexport_types_3_st {
    OSSL_PARAM *priv;
    OSSL_PARAM *pub;
};
#endif

#ifndef ec_imexport_types_3_decoder
static ossl_unused int ec_imexport_types_3_decoder
    (const OSSL_PARAM *p, struct ec_imexport_types_3_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'p':
                switch(s[1]) {
                default:
                    break;
                case 'r':
                    if (ossl_likely(strcmp("iv", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_PRIV_KEY */
                        if (ossl_unlikely(r->priv != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->priv = (OSSL_PARAM *)p;
                    }
                    break;
                case 'u':
                    if (ossl_likely(strcmp("b", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_PUB_KEY */
                        if (ossl_unlikely(r->pub != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->pub = (OSSL_PARAM *)p;
                    }
                }
            }
    return 1;
}
#endif
/* End of machine generated */
#define ec_imexport_types_4_st ec_params_st
/* Machine generated */
#ifndef ec_imexport_types_4_list
static ossl_unused const OSSL_PARAM ec_imexport_types_4_list[] = {
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_GROUP_NAME, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_ENCODING, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_FIELD_TYPE, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_P, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_A, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_B, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_GENERATOR, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_ORDER, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_COFACTOR, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_SEED, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS, NULL),
    OSSL_PARAM_END
};
#endif

#ifndef ec_imexport_types_4_st
struct ec_imexport_types_4_st {
    OSSL_PARAM *a;
    OSSL_PARAM *b;
    OSSL_PARAM *cofactor;
    OSSL_PARAM *decoded;
    OSSL_PARAM *encoding;
    OSSL_PARAM *field_type;
    OSSL_PARAM *generator;
    OSSL_PARAM *group_name;
    OSSL_PARAM *order;
    OSSL_PARAM *p;
    OSSL_PARAM *pt_format;
    OSSL_PARAM *seed;
};
#endif

#ifndef ec_imexport_types_4_decoder
static ossl_unused int ec_imexport_types_4_decoder
    (const OSSL_PARAM *p, struct ec_imexport_types_4_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'a':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->a != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->a = (OSSL_PARAM *)p;
                }
                break;
            case 'b':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->b != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->b = (OSSL_PARAM *)p;
                }
                break;
            case 'c':
                if (ossl_likely(strcmp("ofactor", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_COFACTOR */
                    if (ossl_unlikely(r->cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->cofactor = (OSSL_PARAM *)p;
                }
                break;
            case 'd':
                if (ossl_likely(strcmp("ecoded-from-explicit", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS */
                    if (ossl_unlikely(r->decoded != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->decoded = (OSSL_PARAM *)p;
                }
                break;
            case 'e':
                if (ossl_likely(strcmp("ncoding", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ENCODING */
                    if (ossl_unlikely(r->encoding != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->encoding = (OSSL_PARAM *)p;
                }
                break;
            case 'f':
                if (ossl_likely(strcmp("ield-type", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_FIELD_TYPE */
                    if (ossl_unlikely(r->field_type != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->field_type = (OSSL_PARAM *)p;
                }
                break;
            case 'g':
                switch(s[1]) {
                default:
                    break;
                case 'e':
                    if (ossl_likely(strcmp("nerator", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_GENERATOR */
                        if (ossl_unlikely(r->generator != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->generator = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    if (ossl_likely(strcmp("oup", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_GROUP_NAME */
                        if (ossl_unlikely(r->group_name != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->group_name = (OSSL_PARAM *)p;
                    }
                }
                break;
            case 'o':
                if (ossl_likely(strcmp("rder", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ORDER */
                    if (ossl_unlikely(r->order != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->order = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                switch(s[1]) {
                default:
                    break;
                case 'o':
                    if (ossl_likely(strcmp("int-format", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT */
                        if (ossl_unlikely(r->pt_format != NULL)) {
                            break;
                        }
                        r->pt_format = (OSSL_PARAM *)p;
                    }
                    break;
                case '\0':
                    if (ossl_unlikely(r->p != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->p = (OSSL_PARAM *)p;
                }
                break;
            case 's':
                if (ossl_likely(strcmp("eed", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_SEED */
                    if (ossl_unlikely(r->seed != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->seed = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */
#define ec_imexport_types_5_st ec_params_st
/* Machine generated */
#ifndef ec_imexport_types_5_list
static ossl_unused const OSSL_PARAM ec_imexport_types_5_list[] = {
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_PRIV_KEY, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_GROUP_NAME, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_ENCODING, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_FIELD_TYPE, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_P, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_A, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_B, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_GENERATOR, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_ORDER, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_COFACTOR, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_SEED, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS, NULL),
    OSSL_PARAM_END
};
#endif

#ifndef ec_imexport_types_5_st
struct ec_imexport_types_5_st {
    OSSL_PARAM *a;
    OSSL_PARAM *b;
    OSSL_PARAM *cofactor;
    OSSL_PARAM *decoded;
    OSSL_PARAM *encoding;
    OSSL_PARAM *field_type;
    OSSL_PARAM *generator;
    OSSL_PARAM *group_name;
    OSSL_PARAM *order;
    OSSL_PARAM *p;
    OSSL_PARAM *priv;
    OSSL_PARAM *pt_format;
    OSSL_PARAM *seed;
};
#endif

#ifndef ec_imexport_types_5_decoder
static ossl_unused int ec_imexport_types_5_decoder
    (const OSSL_PARAM *p, struct ec_imexport_types_5_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'a':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->a != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->a = (OSSL_PARAM *)p;
                }
                break;
            case 'b':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->b != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->b = (OSSL_PARAM *)p;
                }
                break;
            case 'c':
                if (ossl_likely(strcmp("ofactor", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_COFACTOR */
                    if (ossl_unlikely(r->cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->cofactor = (OSSL_PARAM *)p;
                }
                break;
            case 'd':
                if (ossl_likely(strcmp("ecoded-from-explicit", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS */
                    if (ossl_unlikely(r->decoded != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->decoded = (OSSL_PARAM *)p;
                }
                break;
            case 'e':
                if (ossl_likely(strcmp("ncoding", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ENCODING */
                    if (ossl_unlikely(r->encoding != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->encoding = (OSSL_PARAM *)p;
                }
                break;
            case 'f':
                if (ossl_likely(strcmp("ield-type", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_FIELD_TYPE */
                    if (ossl_unlikely(r->field_type != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->field_type = (OSSL_PARAM *)p;
                }
                break;
            case 'g':
                switch(s[1]) {
                default:
                    break;
                case 'e':
                    if (ossl_likely(strcmp("nerator", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_GENERATOR */
                        if (ossl_unlikely(r->generator != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->generator = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    if (ossl_likely(strcmp("oup", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_GROUP_NAME */
                        if (ossl_unlikely(r->group_name != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->group_name = (OSSL_PARAM *)p;
                    }
                }
                break;
            case 'o':
                if (ossl_likely(strcmp("rder", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ORDER */
                    if (ossl_unlikely(r->order != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->order = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                switch(s[1]) {
                default:
                    break;
                case 'o':
                    if (ossl_likely(strcmp("int-format", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT */
                        if (ossl_unlikely(r->pt_format != NULL)) {
                            break;
                        }
                        r->pt_format = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    if (ossl_likely(strcmp("iv", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_PRIV_KEY */
                        if (ossl_unlikely(r->priv != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->priv = (OSSL_PARAM *)p;
                    }
                    break;
                case '\0':
                    if (ossl_unlikely(r->p != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->p = (OSSL_PARAM *)p;
                }
                break;
            case 's':
                if (ossl_likely(strcmp("eed", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_SEED */
                    if (ossl_unlikely(r->seed != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->seed = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */
#define ec_imexport_types_6_st ec_params_st
/* Machine generated */
#ifndef ec_imexport_types_6_list
static ossl_unused const OSSL_PARAM ec_imexport_types_6_list[] = {
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_PUB_KEY, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_GROUP_NAME, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_ENCODING, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_FIELD_TYPE, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_P, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_A, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_B, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_GENERATOR, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_ORDER, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_COFACTOR, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_SEED, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS, NULL),
    OSSL_PARAM_END
};
#endif

#ifndef ec_imexport_types_6_st
struct ec_imexport_types_6_st {
    OSSL_PARAM *a;
    OSSL_PARAM *b;
    OSSL_PARAM *cofactor;
    OSSL_PARAM *decoded;
    OSSL_PARAM *encoding;
    OSSL_PARAM *field_type;
    OSSL_PARAM *generator;
    OSSL_PARAM *group_name;
    OSSL_PARAM *order;
    OSSL_PARAM *p;
    OSSL_PARAM *pt_format;
    OSSL_PARAM *pub;
    OSSL_PARAM *seed;
};
#endif

#ifndef ec_imexport_types_6_decoder
static ossl_unused int ec_imexport_types_6_decoder
    (const OSSL_PARAM *p, struct ec_imexport_types_6_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'a':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->a != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->a = (OSSL_PARAM *)p;
                }
                break;
            case 'b':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->b != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->b = (OSSL_PARAM *)p;
                }
                break;
            case 'c':
                if (ossl_likely(strcmp("ofactor", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_COFACTOR */
                    if (ossl_unlikely(r->cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->cofactor = (OSSL_PARAM *)p;
                }
                break;
            case 'd':
                if (ossl_likely(strcmp("ecoded-from-explicit", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS */
                    if (ossl_unlikely(r->decoded != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->decoded = (OSSL_PARAM *)p;
                }
                break;
            case 'e':
                if (ossl_likely(strcmp("ncoding", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ENCODING */
                    if (ossl_unlikely(r->encoding != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->encoding = (OSSL_PARAM *)p;
                }
                break;
            case 'f':
                if (ossl_likely(strcmp("ield-type", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_FIELD_TYPE */
                    if (ossl_unlikely(r->field_type != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->field_type = (OSSL_PARAM *)p;
                }
                break;
            case 'g':
                switch(s[1]) {
                default:
                    break;
                case 'e':
                    if (ossl_likely(strcmp("nerator", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_GENERATOR */
                        if (ossl_unlikely(r->generator != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->generator = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    if (ossl_likely(strcmp("oup", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_GROUP_NAME */
                        if (ossl_unlikely(r->group_name != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->group_name = (OSSL_PARAM *)p;
                    }
                }
                break;
            case 'o':
                if (ossl_likely(strcmp("rder", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ORDER */
                    if (ossl_unlikely(r->order != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->order = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                switch(s[1]) {
                default:
                    break;
                case 'o':
                    if (ossl_likely(strcmp("int-format", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT */
                        if (ossl_unlikely(r->pt_format != NULL)) {
                            break;
                        }
                        r->pt_format = (OSSL_PARAM *)p;
                    }
                    break;
                case 'u':
                    if (ossl_likely(strcmp("b", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_PUB_KEY */
                        if (ossl_unlikely(r->pub != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->pub = (OSSL_PARAM *)p;
                    }
                    break;
                case '\0':
                    if (ossl_unlikely(r->p != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->p = (OSSL_PARAM *)p;
                }
                break;
            case 's':
                if (ossl_likely(strcmp("eed", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_SEED */
                    if (ossl_unlikely(r->seed != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->seed = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */
#define ec_imexport_types_7_st ec_params_st
/* Machine generated */
#ifndef ec_imexport_types_7_list
static ossl_unused const OSSL_PARAM ec_imexport_types_7_list[] = {
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_PRIV_KEY, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_PUB_KEY, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_GROUP_NAME, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_ENCODING, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_FIELD_TYPE, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_P, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_A, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_B, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_GENERATOR, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_ORDER, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_COFACTOR, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_SEED, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS, NULL),
    OSSL_PARAM_END
};
#endif

#ifndef ec_imexport_types_7_st
struct ec_imexport_types_7_st {
    OSSL_PARAM *a;
    OSSL_PARAM *b;
    OSSL_PARAM *cofactor;
    OSSL_PARAM *decoded;
    OSSL_PARAM *encoding;
    OSSL_PARAM *field_type;
    OSSL_PARAM *generator;
    OSSL_PARAM *group_name;
    OSSL_PARAM *order;
    OSSL_PARAM *p;
    OSSL_PARAM *priv;
    OSSL_PARAM *pt_format;
    OSSL_PARAM *pub;
    OSSL_PARAM *seed;
};
#endif

#ifndef ec_imexport_types_7_decoder
static ossl_unused int ec_imexport_types_7_decoder
    (const OSSL_PARAM *p, struct ec_imexport_types_7_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'a':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->a != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->a = (OSSL_PARAM *)p;
                }
                break;
            case 'b':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->b != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->b = (OSSL_PARAM *)p;
                }
                break;
            case 'c':
                if (ossl_likely(strcmp("ofactor", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_COFACTOR */
                    if (ossl_unlikely(r->cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->cofactor = (OSSL_PARAM *)p;
                }
                break;
            case 'd':
                if (ossl_likely(strcmp("ecoded-from-explicit", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS */
                    if (ossl_unlikely(r->decoded != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->decoded = (OSSL_PARAM *)p;
                }
                break;
            case 'e':
                if (ossl_likely(strcmp("ncoding", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ENCODING */
                    if (ossl_unlikely(r->encoding != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->encoding = (OSSL_PARAM *)p;
                }
                break;
            case 'f':
                if (ossl_likely(strcmp("ield-type", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_FIELD_TYPE */
                    if (ossl_unlikely(r->field_type != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->field_type = (OSSL_PARAM *)p;
                }
                break;
            case 'g':
                switch(s[1]) {
                default:
                    break;
                case 'e':
                    if (ossl_likely(strcmp("nerator", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_GENERATOR */
                        if (ossl_unlikely(r->generator != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->generator = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    if (ossl_likely(strcmp("oup", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_GROUP_NAME */
                        if (ossl_unlikely(r->group_name != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->group_name = (OSSL_PARAM *)p;
                    }
                }
                break;
            case 'o':
                if (ossl_likely(strcmp("rder", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ORDER */
                    if (ossl_unlikely(r->order != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->order = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                switch(s[1]) {
                default:
                    break;
                case 'o':
                    if (ossl_likely(strcmp("int-format", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT */
                        if (ossl_unlikely(r->pt_format != NULL)) {
                            break;
                        }
                        r->pt_format = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    if (ossl_likely(strcmp("iv", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_PRIV_KEY */
                        if (ossl_unlikely(r->priv != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->priv = (OSSL_PARAM *)p;
                    }
                    break;
                case 'u':
                    if (ossl_likely(strcmp("b", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_PUB_KEY */
                        if (ossl_unlikely(r->pub != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->pub = (OSSL_PARAM *)p;
                    }
                    break;
                case '\0':
                    if (ossl_unlikely(r->p != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->p = (OSSL_PARAM *)p;
                }
                break;
            case 's':
                if (ossl_likely(strcmp("eed", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_SEED */
                    if (ossl_unlikely(r->seed != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->seed = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */
#define ec_imexport_types_8_st ec_params_st
/* Machine generated */
#ifndef ec_imexport_types_8_list
static ossl_unused const OSSL_PARAM ec_imexport_types_8_list[] = {
    OSSL_PARAM_int(OSSL_PKEY_PARAM_USE_COFACTOR_ECDH, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC, NULL),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE, NULL, 0),
    OSSL_PARAM_END
};
#endif

#ifndef ec_imexport_types_8_st
struct ec_imexport_types_8_st {
    OSSL_PARAM *group_check;
    OSSL_PARAM *include_public;
    OSSL_PARAM *use_cofactor;
};
#endif

#ifndef ec_imexport_types_8_decoder
static ossl_unused int ec_imexport_types_8_decoder
    (const OSSL_PARAM *p, struct ec_imexport_types_8_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'g':
                if (ossl_likely(strcmp("roup-check", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE */
                    if (ossl_unlikely(r->group_check != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->group_check = (OSSL_PARAM *)p;
                }
                break;
            case 'i':
                if (ossl_likely(strcmp("nclude-public", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC */
                    if (ossl_unlikely(r->include_public != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->include_public = (OSSL_PARAM *)p;
                }
                break;
            case 'u':
                if (ossl_likely(strcmp("se-cofactor-flag", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_USE_COFACTOR_ECDH */
                    if (ossl_unlikely(r->use_cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->use_cofactor = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */
#define ec_imexport_types_9_st ec_params_st
/* Machine generated */
#ifndef ec_imexport_types_9_list
static ossl_unused const OSSL_PARAM ec_imexport_types_9_list[] = {
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_PRIV_KEY, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_USE_COFACTOR_ECDH, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC, NULL),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE, NULL, 0),
    OSSL_PARAM_END
};
#endif

#ifndef ec_imexport_types_9_st
struct ec_imexport_types_9_st {
    OSSL_PARAM *group_check;
    OSSL_PARAM *include_public;
    OSSL_PARAM *priv;
    OSSL_PARAM *use_cofactor;
};
#endif

#ifndef ec_imexport_types_9_decoder
static ossl_unused int ec_imexport_types_9_decoder
    (const OSSL_PARAM *p, struct ec_imexport_types_9_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'g':
                if (ossl_likely(strcmp("roup-check", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE */
                    if (ossl_unlikely(r->group_check != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->group_check = (OSSL_PARAM *)p;
                }
                break;
            case 'i':
                if (ossl_likely(strcmp("nclude-public", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC */
                    if (ossl_unlikely(r->include_public != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->include_public = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                if (ossl_likely(strcmp("riv", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_PRIV_KEY */
                    if (ossl_unlikely(r->priv != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->priv = (OSSL_PARAM *)p;
                }
                break;
            case 'u':
                if (ossl_likely(strcmp("se-cofactor-flag", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_USE_COFACTOR_ECDH */
                    if (ossl_unlikely(r->use_cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->use_cofactor = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */
#define ec_imexport_types_10_st ec_params_st
/* Machine generated */
#ifndef ec_imexport_types_10_list
static ossl_unused const OSSL_PARAM ec_imexport_types_10_list[] = {
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_PUB_KEY, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_USE_COFACTOR_ECDH, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC, NULL),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE, NULL, 0),
    OSSL_PARAM_END
};
#endif

#ifndef ec_imexport_types_10_st
struct ec_imexport_types_10_st {
    OSSL_PARAM *group_check;
    OSSL_PARAM *include_public;
    OSSL_PARAM *pub;
    OSSL_PARAM *use_cofactor;
};
#endif

#ifndef ec_imexport_types_10_decoder
static ossl_unused int ec_imexport_types_10_decoder
    (const OSSL_PARAM *p, struct ec_imexport_types_10_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'g':
                if (ossl_likely(strcmp("roup-check", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE */
                    if (ossl_unlikely(r->group_check != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->group_check = (OSSL_PARAM *)p;
                }
                break;
            case 'i':
                if (ossl_likely(strcmp("nclude-public", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC */
                    if (ossl_unlikely(r->include_public != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->include_public = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                if (ossl_likely(strcmp("ub", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_PUB_KEY */
                    if (ossl_unlikely(r->pub != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->pub = (OSSL_PARAM *)p;
                }
                break;
            case 'u':
                if (ossl_likely(strcmp("se-cofactor-flag", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_USE_COFACTOR_ECDH */
                    if (ossl_unlikely(r->use_cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->use_cofactor = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */
#define ec_imexport_types_11_st ec_params_st
/* Machine generated */
#ifndef ec_imexport_types_11_list
static ossl_unused const OSSL_PARAM ec_imexport_types_11_list[] = {
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_PRIV_KEY, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_PUB_KEY, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_USE_COFACTOR_ECDH, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC, NULL),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE, NULL, 0),
    OSSL_PARAM_END
};
#endif

#ifndef ec_imexport_types_11_st
struct ec_imexport_types_11_st {
    OSSL_PARAM *group_check;
    OSSL_PARAM *include_public;
    OSSL_PARAM *priv;
    OSSL_PARAM *pub;
    OSSL_PARAM *use_cofactor;
};
#endif

#ifndef ec_imexport_types_11_decoder
static ossl_unused int ec_imexport_types_11_decoder
    (const OSSL_PARAM *p, struct ec_imexport_types_11_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'g':
                if (ossl_likely(strcmp("roup-check", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE */
                    if (ossl_unlikely(r->group_check != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->group_check = (OSSL_PARAM *)p;
                }
                break;
            case 'i':
                if (ossl_likely(strcmp("nclude-public", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC */
                    if (ossl_unlikely(r->include_public != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->include_public = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                switch(s[1]) {
                default:
                    break;
                case 'r':
                    if (ossl_likely(strcmp("iv", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_PRIV_KEY */
                        if (ossl_unlikely(r->priv != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->priv = (OSSL_PARAM *)p;
                    }
                    break;
                case 'u':
                    if (ossl_likely(strcmp("b", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_PUB_KEY */
                        if (ossl_unlikely(r->pub != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->pub = (OSSL_PARAM *)p;
                    }
                }
                break;
            case 'u':
                if (ossl_likely(strcmp("se-cofactor-flag", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_USE_COFACTOR_ECDH */
                    if (ossl_unlikely(r->use_cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->use_cofactor = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */
#define ec_imexport_types_12_st ec_params_st
/* Machine generated */
#ifndef ec_imexport_types_12_list
static ossl_unused const OSSL_PARAM ec_imexport_types_12_list[] = {
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_GROUP_NAME, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_ENCODING, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_FIELD_TYPE, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_P, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_A, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_B, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_GENERATOR, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_ORDER, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_COFACTOR, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_SEED, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_USE_COFACTOR_ECDH, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC, NULL),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE, NULL, 0),
    OSSL_PARAM_END
};
#endif

#ifndef ec_imexport_types_12_st
struct ec_imexport_types_12_st {
    OSSL_PARAM *a;
    OSSL_PARAM *b;
    OSSL_PARAM *cofactor;
    OSSL_PARAM *decoded;
    OSSL_PARAM *encoding;
    OSSL_PARAM *field_type;
    OSSL_PARAM *generator;
    OSSL_PARAM *group_check;
    OSSL_PARAM *group_name;
    OSSL_PARAM *include_public;
    OSSL_PARAM *order;
    OSSL_PARAM *p;
    OSSL_PARAM *pt_format;
    OSSL_PARAM *seed;
    OSSL_PARAM *use_cofactor;
};
#endif

#ifndef ec_imexport_types_12_decoder
static ossl_unused int ec_imexport_types_12_decoder
    (const OSSL_PARAM *p, struct ec_imexport_types_12_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'a':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->a != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->a = (OSSL_PARAM *)p;
                }
                break;
            case 'b':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->b != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->b = (OSSL_PARAM *)p;
                }
                break;
            case 'c':
                if (ossl_likely(strcmp("ofactor", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_COFACTOR */
                    if (ossl_unlikely(r->cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->cofactor = (OSSL_PARAM *)p;
                }
                break;
            case 'd':
                if (ossl_likely(strcmp("ecoded-from-explicit", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS */
                    if (ossl_unlikely(r->decoded != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->decoded = (OSSL_PARAM *)p;
                }
                break;
            case 'e':
                if (ossl_likely(strcmp("ncoding", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ENCODING */
                    if (ossl_unlikely(r->encoding != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->encoding = (OSSL_PARAM *)p;
                }
                break;
            case 'f':
                if (ossl_likely(strcmp("ield-type", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_FIELD_TYPE */
                    if (ossl_unlikely(r->field_type != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->field_type = (OSSL_PARAM *)p;
                }
                break;
            case 'g':
                switch(s[1]) {
                default:
                    break;
                case 'e':
                    if (ossl_likely(strcmp("nerator", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_GENERATOR */
                        if (ossl_unlikely(r->generator != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->generator = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    switch(s[2]) {
                    default:
                        break;
                    case 'o':
                        switch(s[3]) {
                        default:
                            break;
                        case 'u':
                            switch(s[4]) {
                            default:
                                break;
                            case 'p':
                                switch(s[5]) {
                                default:
                                    break;
                                case '-':
                                    if (ossl_likely(strcmp("check", s + 6) == 0)) {
                                        /* OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE */
                                        if (ossl_unlikely(r->group_check != NULL)) {
                                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                           "param %s is repeated", s);
                                            return 0;
                                        }
                                        r->group_check = (OSSL_PARAM *)p;
                                    }
                                    break;
                                case '\0':
                                    if (ossl_unlikely(r->group_name != NULL)) {
                                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                       "param %s is repeated", s);
                                        return 0;
                                    }
                                    r->group_name = (OSSL_PARAM *)p;
                                }
                            }
                        }
                    }
                }
                break;
            case 'i':
                if (ossl_likely(strcmp("nclude-public", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC */
                    if (ossl_unlikely(r->include_public != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->include_public = (OSSL_PARAM *)p;
                }
                break;
            case 'o':
                if (ossl_likely(strcmp("rder", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ORDER */
                    if (ossl_unlikely(r->order != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->order = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                switch(s[1]) {
                default:
                    break;
                case 'o':
                    if (ossl_likely(strcmp("int-format", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT */
                        if (ossl_unlikely(r->pt_format != NULL)) {
                            break;
                        }
                        r->pt_format = (OSSL_PARAM *)p;
                    }
                    break;
                case '\0':
                    if (ossl_unlikely(r->p != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->p = (OSSL_PARAM *)p;
                }
                break;
            case 's':
                if (ossl_likely(strcmp("eed", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_SEED */
                    if (ossl_unlikely(r->seed != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->seed = (OSSL_PARAM *)p;
                }
                break;
            case 'u':
                if (ossl_likely(strcmp("se-cofactor-flag", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_USE_COFACTOR_ECDH */
                    if (ossl_unlikely(r->use_cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->use_cofactor = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */
#define ec_imexport_types_13_st ec_params_st
/* Machine generated */
#ifndef ec_imexport_types_13_list
static ossl_unused const OSSL_PARAM ec_imexport_types_13_list[] = {
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_PRIV_KEY, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_GROUP_NAME, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_ENCODING, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_FIELD_TYPE, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_P, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_A, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_B, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_GENERATOR, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_ORDER, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_COFACTOR, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_SEED, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_USE_COFACTOR_ECDH, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC, NULL),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE, NULL, 0),
    OSSL_PARAM_END
};
#endif

#ifndef ec_imexport_types_13_st
struct ec_imexport_types_13_st {
    OSSL_PARAM *a;
    OSSL_PARAM *b;
    OSSL_PARAM *cofactor;
    OSSL_PARAM *decoded;
    OSSL_PARAM *encoding;
    OSSL_PARAM *field_type;
    OSSL_PARAM *generator;
    OSSL_PARAM *group_check;
    OSSL_PARAM *group_name;
    OSSL_PARAM *include_public;
    OSSL_PARAM *order;
    OSSL_PARAM *p;
    OSSL_PARAM *priv;
    OSSL_PARAM *pt_format;
    OSSL_PARAM *seed;
    OSSL_PARAM *use_cofactor;
};
#endif

#ifndef ec_imexport_types_13_decoder
static ossl_unused int ec_imexport_types_13_decoder
    (const OSSL_PARAM *p, struct ec_imexport_types_13_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'a':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->a != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->a = (OSSL_PARAM *)p;
                }
                break;
            case 'b':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->b != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->b = (OSSL_PARAM *)p;
                }
                break;
            case 'c':
                if (ossl_likely(strcmp("ofactor", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_COFACTOR */
                    if (ossl_unlikely(r->cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->cofactor = (OSSL_PARAM *)p;
                }
                break;
            case 'd':
                if (ossl_likely(strcmp("ecoded-from-explicit", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS */
                    if (ossl_unlikely(r->decoded != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->decoded = (OSSL_PARAM *)p;
                }
                break;
            case 'e':
                if (ossl_likely(strcmp("ncoding", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ENCODING */
                    if (ossl_unlikely(r->encoding != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->encoding = (OSSL_PARAM *)p;
                }
                break;
            case 'f':
                if (ossl_likely(strcmp("ield-type", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_FIELD_TYPE */
                    if (ossl_unlikely(r->field_type != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->field_type = (OSSL_PARAM *)p;
                }
                break;
            case 'g':
                switch(s[1]) {
                default:
                    break;
                case 'e':
                    if (ossl_likely(strcmp("nerator", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_GENERATOR */
                        if (ossl_unlikely(r->generator != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->generator = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    switch(s[2]) {
                    default:
                        break;
                    case 'o':
                        switch(s[3]) {
                        default:
                            break;
                        case 'u':
                            switch(s[4]) {
                            default:
                                break;
                            case 'p':
                                switch(s[5]) {
                                default:
                                    break;
                                case '-':
                                    if (ossl_likely(strcmp("check", s + 6) == 0)) {
                                        /* OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE */
                                        if (ossl_unlikely(r->group_check != NULL)) {
                                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                           "param %s is repeated", s);
                                            return 0;
                                        }
                                        r->group_check = (OSSL_PARAM *)p;
                                    }
                                    break;
                                case '\0':
                                    if (ossl_unlikely(r->group_name != NULL)) {
                                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                       "param %s is repeated", s);
                                        return 0;
                                    }
                                    r->group_name = (OSSL_PARAM *)p;
                                }
                            }
                        }
                    }
                }
                break;
            case 'i':
                if (ossl_likely(strcmp("nclude-public", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC */
                    if (ossl_unlikely(r->include_public != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->include_public = (OSSL_PARAM *)p;
                }
                break;
            case 'o':
                if (ossl_likely(strcmp("rder", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ORDER */
                    if (ossl_unlikely(r->order != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->order = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                switch(s[1]) {
                default:
                    break;
                case 'o':
                    if (ossl_likely(strcmp("int-format", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT */
                        if (ossl_unlikely(r->pt_format != NULL)) {
                            break;
                        }
                        r->pt_format = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    if (ossl_likely(strcmp("iv", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_PRIV_KEY */
                        if (ossl_unlikely(r->priv != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->priv = (OSSL_PARAM *)p;
                    }
                    break;
                case '\0':
                    if (ossl_unlikely(r->p != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->p = (OSSL_PARAM *)p;
                }
                break;
            case 's':
                if (ossl_likely(strcmp("eed", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_SEED */
                    if (ossl_unlikely(r->seed != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->seed = (OSSL_PARAM *)p;
                }
                break;
            case 'u':
                if (ossl_likely(strcmp("se-cofactor-flag", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_USE_COFACTOR_ECDH */
                    if (ossl_unlikely(r->use_cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->use_cofactor = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */
#define ec_imexport_types_14_st ec_params_st
/* Machine generated */
#ifndef ec_imexport_types_14_list
static ossl_unused const OSSL_PARAM ec_imexport_types_14_list[] = {
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_PUB_KEY, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_GROUP_NAME, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_ENCODING, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_FIELD_TYPE, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_P, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_A, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_B, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_GENERATOR, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_ORDER, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_COFACTOR, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_SEED, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_USE_COFACTOR_ECDH, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC, NULL),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE, NULL, 0),
    OSSL_PARAM_END
};
#endif

#ifndef ec_imexport_types_14_st
struct ec_imexport_types_14_st {
    OSSL_PARAM *a;
    OSSL_PARAM *b;
    OSSL_PARAM *cofactor;
    OSSL_PARAM *decoded;
    OSSL_PARAM *encoding;
    OSSL_PARAM *field_type;
    OSSL_PARAM *generator;
    OSSL_PARAM *group_check;
    OSSL_PARAM *group_name;
    OSSL_PARAM *include_public;
    OSSL_PARAM *order;
    OSSL_PARAM *p;
    OSSL_PARAM *pt_format;
    OSSL_PARAM *pub;
    OSSL_PARAM *seed;
    OSSL_PARAM *use_cofactor;
};
#endif

#ifndef ec_imexport_types_14_decoder
static ossl_unused int ec_imexport_types_14_decoder
    (const OSSL_PARAM *p, struct ec_imexport_types_14_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'a':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->a != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->a = (OSSL_PARAM *)p;
                }
                break;
            case 'b':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->b != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->b = (OSSL_PARAM *)p;
                }
                break;
            case 'c':
                if (ossl_likely(strcmp("ofactor", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_COFACTOR */
                    if (ossl_unlikely(r->cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->cofactor = (OSSL_PARAM *)p;
                }
                break;
            case 'd':
                if (ossl_likely(strcmp("ecoded-from-explicit", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS */
                    if (ossl_unlikely(r->decoded != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->decoded = (OSSL_PARAM *)p;
                }
                break;
            case 'e':
                if (ossl_likely(strcmp("ncoding", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ENCODING */
                    if (ossl_unlikely(r->encoding != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->encoding = (OSSL_PARAM *)p;
                }
                break;
            case 'f':
                if (ossl_likely(strcmp("ield-type", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_FIELD_TYPE */
                    if (ossl_unlikely(r->field_type != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->field_type = (OSSL_PARAM *)p;
                }
                break;
            case 'g':
                switch(s[1]) {
                default:
                    break;
                case 'e':
                    if (ossl_likely(strcmp("nerator", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_GENERATOR */
                        if (ossl_unlikely(r->generator != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->generator = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    switch(s[2]) {
                    default:
                        break;
                    case 'o':
                        switch(s[3]) {
                        default:
                            break;
                        case 'u':
                            switch(s[4]) {
                            default:
                                break;
                            case 'p':
                                switch(s[5]) {
                                default:
                                    break;
                                case '-':
                                    if (ossl_likely(strcmp("check", s + 6) == 0)) {
                                        /* OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE */
                                        if (ossl_unlikely(r->group_check != NULL)) {
                                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                           "param %s is repeated", s);
                                            return 0;
                                        }
                                        r->group_check = (OSSL_PARAM *)p;
                                    }
                                    break;
                                case '\0':
                                    if (ossl_unlikely(r->group_name != NULL)) {
                                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                       "param %s is repeated", s);
                                        return 0;
                                    }
                                    r->group_name = (OSSL_PARAM *)p;
                                }
                            }
                        }
                    }
                }
                break;
            case 'i':
                if (ossl_likely(strcmp("nclude-public", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC */
                    if (ossl_unlikely(r->include_public != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->include_public = (OSSL_PARAM *)p;
                }
                break;
            case 'o':
                if (ossl_likely(strcmp("rder", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ORDER */
                    if (ossl_unlikely(r->order != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->order = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                switch(s[1]) {
                default:
                    break;
                case 'o':
                    if (ossl_likely(strcmp("int-format", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT */
                        if (ossl_unlikely(r->pt_format != NULL)) {
                            break;
                        }
                        r->pt_format = (OSSL_PARAM *)p;
                    }
                    break;
                case 'u':
                    if (ossl_likely(strcmp("b", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_PUB_KEY */
                        if (ossl_unlikely(r->pub != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->pub = (OSSL_PARAM *)p;
                    }
                    break;
                case '\0':
                    if (ossl_unlikely(r->p != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->p = (OSSL_PARAM *)p;
                }
                break;
            case 's':
                if (ossl_likely(strcmp("eed", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_SEED */
                    if (ossl_unlikely(r->seed != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->seed = (OSSL_PARAM *)p;
                }
                break;
            case 'u':
                if (ossl_likely(strcmp("se-cofactor-flag", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_USE_COFACTOR_ECDH */
                    if (ossl_unlikely(r->use_cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->use_cofactor = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */
#define ec_imexport_types_15_st ec_params_st
/* Machine generated */
#ifndef ec_imexport_types_15_list
static ossl_unused const OSSL_PARAM ec_imexport_types_15_list[] = {
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_PRIV_KEY, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_PUB_KEY, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_GROUP_NAME, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_ENCODING, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_FIELD_TYPE, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_P, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_A, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_B, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_GENERATOR, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_ORDER, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_COFACTOR, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_SEED, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_USE_COFACTOR_ECDH, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC, NULL),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE, NULL, 0),
    OSSL_PARAM_END
};
#endif

#ifndef ec_imexport_types_15_st
struct ec_imexport_types_15_st {
    OSSL_PARAM *a;
    OSSL_PARAM *b;
    OSSL_PARAM *cofactor;
    OSSL_PARAM *decoded;
    OSSL_PARAM *encoding;
    OSSL_PARAM *field_type;
    OSSL_PARAM *generator;
    OSSL_PARAM *group_check;
    OSSL_PARAM *group_name;
    OSSL_PARAM *include_public;
    OSSL_PARAM *order;
    OSSL_PARAM *p;
    OSSL_PARAM *priv;
    OSSL_PARAM *pt_format;
    OSSL_PARAM *pub;
    OSSL_PARAM *seed;
    OSSL_PARAM *use_cofactor;
};
#endif

#ifndef ec_imexport_types_15_decoder
static ossl_unused int ec_imexport_types_15_decoder
    (const OSSL_PARAM *p, struct ec_imexport_types_15_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'a':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->a != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->a = (OSSL_PARAM *)p;
                }
                break;
            case 'b':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->b != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->b = (OSSL_PARAM *)p;
                }
                break;
            case 'c':
                if (ossl_likely(strcmp("ofactor", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_COFACTOR */
                    if (ossl_unlikely(r->cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->cofactor = (OSSL_PARAM *)p;
                }
                break;
            case 'd':
                if (ossl_likely(strcmp("ecoded-from-explicit", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS */
                    if (ossl_unlikely(r->decoded != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->decoded = (OSSL_PARAM *)p;
                }
                break;
            case 'e':
                if (ossl_likely(strcmp("ncoding", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ENCODING */
                    if (ossl_unlikely(r->encoding != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->encoding = (OSSL_PARAM *)p;
                }
                break;
            case 'f':
                if (ossl_likely(strcmp("ield-type", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_FIELD_TYPE */
                    if (ossl_unlikely(r->field_type != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->field_type = (OSSL_PARAM *)p;
                }
                break;
            case 'g':
                switch(s[1]) {
                default:
                    break;
                case 'e':
                    if (ossl_likely(strcmp("nerator", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_GENERATOR */
                        if (ossl_unlikely(r->generator != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->generator = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    switch(s[2]) {
                    default:
                        break;
                    case 'o':
                        switch(s[3]) {
                        default:
                            break;
                        case 'u':
                            switch(s[4]) {
                            default:
                                break;
                            case 'p':
                                switch(s[5]) {
                                default:
                                    break;
                                case '-':
                                    if (ossl_likely(strcmp("check", s + 6) == 0)) {
                                        /* OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE */
                                        if (ossl_unlikely(r->group_check != NULL)) {
                                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                           "param %s is repeated", s);
                                            return 0;
                                        }
                                        r->group_check = (OSSL_PARAM *)p;
                                    }
                                    break;
                                case '\0':
                                    if (ossl_unlikely(r->group_name != NULL)) {
                                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                       "param %s is repeated", s);
                                        return 0;
                                    }
                                    r->group_name = (OSSL_PARAM *)p;
                                }
                            }
                        }
                    }
                }
                break;
            case 'i':
                if (ossl_likely(strcmp("nclude-public", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC */
                    if (ossl_unlikely(r->include_public != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->include_public = (OSSL_PARAM *)p;
                }
                break;
            case 'o':
                if (ossl_likely(strcmp("rder", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ORDER */
                    if (ossl_unlikely(r->order != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->order = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                switch(s[1]) {
                default:
                    break;
                case 'o':
                    if (ossl_likely(strcmp("int-format", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT */
                        if (ossl_unlikely(r->pt_format != NULL)) {
                            break;
                        }
                        r->pt_format = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    if (ossl_likely(strcmp("iv", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_PRIV_KEY */
                        if (ossl_unlikely(r->priv != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->priv = (OSSL_PARAM *)p;
                    }
                    break;
                case 'u':
                    if (ossl_likely(strcmp("b", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_PUB_KEY */
                        if (ossl_unlikely(r->pub != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->pub = (OSSL_PARAM *)p;
                    }
                    break;
                case '\0':
                    if (ossl_unlikely(r->p != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->p = (OSSL_PARAM *)p;
                }
                break;
            case 's':
                if (ossl_likely(strcmp("eed", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_SEED */
                    if (ossl_unlikely(r->seed != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->seed = (OSSL_PARAM *)p;
                }
                break;
            case 'u':
                if (ossl_likely(strcmp("se-cofactor-flag", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_USE_COFACTOR_ECDH */
                    if (ossl_unlikely(r->use_cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->use_cofactor = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */


/* Public EC compatibility functions retain their existing signatures. */
#define ec_group_fromdata_st ec_params_st
/* Machine generated */
#ifndef ec_group_fromdata_list
static ossl_unused const OSSL_PARAM ec_group_fromdata_list[] = {
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_GROUP_NAME, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_ENCODING, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_FIELD_TYPE, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_P, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_A, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_B, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_GENERATOR, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_ORDER, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_COFACTOR, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_SEED, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS, NULL),
    OSSL_PARAM_END
};
#endif

#ifndef ec_group_fromdata_st
struct ec_group_fromdata_st {
    OSSL_PARAM *a;
    OSSL_PARAM *b;
    OSSL_PARAM *cofactor;
    OSSL_PARAM *decoded;
    OSSL_PARAM *encoding;
    OSSL_PARAM *field_type;
    OSSL_PARAM *generator;
    OSSL_PARAM *group_name;
    OSSL_PARAM *order;
    OSSL_PARAM *p;
    OSSL_PARAM *pt_format;
    OSSL_PARAM *seed;
};
#endif

#ifndef ec_group_fromdata_decoder
static ossl_unused int ec_group_fromdata_decoder
    (const OSSL_PARAM *p, struct ec_group_fromdata_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'a':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->a != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->a = (OSSL_PARAM *)p;
                }
                break;
            case 'b':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->b != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->b = (OSSL_PARAM *)p;
                }
                break;
            case 'c':
                if (ossl_likely(strcmp("ofactor", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_COFACTOR */
                    if (ossl_unlikely(r->cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->cofactor = (OSSL_PARAM *)p;
                }
                break;
            case 'd':
                if (ossl_likely(strcmp("ecoded-from-explicit", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS */
                    if (ossl_unlikely(r->decoded != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->decoded = (OSSL_PARAM *)p;
                }
                break;
            case 'e':
                if (ossl_likely(strcmp("ncoding", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ENCODING */
                    if (ossl_unlikely(r->encoding != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->encoding = (OSSL_PARAM *)p;
                }
                break;
            case 'f':
                if (ossl_likely(strcmp("ield-type", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_FIELD_TYPE */
                    if (ossl_unlikely(r->field_type != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->field_type = (OSSL_PARAM *)p;
                }
                break;
            case 'g':
                switch(s[1]) {
                default:
                    break;
                case 'e':
                    if (ossl_likely(strcmp("nerator", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_GENERATOR */
                        if (ossl_unlikely(r->generator != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->generator = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    if (ossl_likely(strcmp("oup", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_GROUP_NAME */
                        if (ossl_unlikely(r->group_name != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->group_name = (OSSL_PARAM *)p;
                    }
                }
                break;
            case 'o':
                if (ossl_likely(strcmp("rder", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ORDER */
                    if (ossl_unlikely(r->order != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->order = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                switch(s[1]) {
                default:
                    break;
                case 'o':
                    if (ossl_likely(strcmp("int-format", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT */
                        if (ossl_unlikely(r->pt_format != NULL)) {
                            break;
                        }
                        r->pt_format = (OSSL_PARAM *)p;
                    }
                    break;
                case '\0':
                    if (ossl_unlikely(r->p != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->p = (OSSL_PARAM *)p;
                }
                break;
            case 's':
                if (ossl_likely(strcmp("eed", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_SEED */
                    if (ossl_unlikely(r->seed != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->seed = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */

#define ec_group_todata_st ec_params_st
/* Machine generated */
#ifndef ec_group_todata_list
static ossl_unused const OSSL_PARAM ec_group_todata_list[] = {
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_GROUP_NAME, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_ENCODING, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_FIELD_TYPE, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_P, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_A, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_B, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_GENERATOR, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_ORDER, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_COFACTOR, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_SEED, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS, NULL),
    OSSL_PARAM_END
};
#endif

#ifndef ec_group_todata_st
struct ec_group_todata_st {
    OSSL_PARAM *a;
    OSSL_PARAM *b;
    OSSL_PARAM *cofactor;
    OSSL_PARAM *decoded;
    OSSL_PARAM *encoding;
    OSSL_PARAM *field_type;
    OSSL_PARAM *generator;
    OSSL_PARAM *group_name;
    OSSL_PARAM *order;
    OSSL_PARAM *p;
    OSSL_PARAM *pt_format;
    OSSL_PARAM *seed;
};
#endif

#ifndef ec_group_todata_decoder
static ossl_unused int ec_group_todata_decoder
    (const OSSL_PARAM *p, struct ec_group_todata_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'a':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->a != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->a = (OSSL_PARAM *)p;
                }
                break;
            case 'b':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->b != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->b = (OSSL_PARAM *)p;
                }
                break;
            case 'c':
                if (ossl_likely(strcmp("ofactor", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_COFACTOR */
                    if (ossl_unlikely(r->cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->cofactor = (OSSL_PARAM *)p;
                }
                break;
            case 'd':
                if (ossl_likely(strcmp("ecoded-from-explicit", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS */
                    if (ossl_unlikely(r->decoded != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->decoded = (OSSL_PARAM *)p;
                }
                break;
            case 'e':
                if (ossl_likely(strcmp("ncoding", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ENCODING */
                    if (ossl_unlikely(r->encoding != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->encoding = (OSSL_PARAM *)p;
                }
                break;
            case 'f':
                if (ossl_likely(strcmp("ield-type", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_FIELD_TYPE */
                    if (ossl_unlikely(r->field_type != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->field_type = (OSSL_PARAM *)p;
                }
                break;
            case 'g':
                switch(s[1]) {
                default:
                    break;
                case 'e':
                    if (ossl_likely(strcmp("nerator", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_GENERATOR */
                        if (ossl_unlikely(r->generator != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->generator = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    if (ossl_likely(strcmp("oup", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_GROUP_NAME */
                        if (ossl_unlikely(r->group_name != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->group_name = (OSSL_PARAM *)p;
                    }
                }
                break;
            case 'o':
                if (ossl_likely(strcmp("rder", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ORDER */
                    if (ossl_unlikely(r->order != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->order = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                switch(s[1]) {
                default:
                    break;
                case 'o':
                    if (ossl_likely(strcmp("int-format", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT */
                        if (ossl_unlikely(r->pt_format != NULL)) {
                            break;
                        }
                        r->pt_format = (OSSL_PARAM *)p;
                    }
                    break;
                case '\0':
                    if (ossl_unlikely(r->p != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->p = (OSSL_PARAM *)p;
                }
                break;
            case 's':
                if (ossl_likely(strcmp("eed", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_SEED */
                    if (ossl_unlikely(r->seed != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->seed = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */

#define ec_key_fromdata_st ec_params_st
/* Machine generated */
#ifndef ec_key_fromdata_list
static ossl_unused const OSSL_PARAM ec_key_fromdata_list[] = {
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_PUB_KEY, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_PRIV_KEY, NULL, 0),
    OSSL_PARAM_END
};
#endif

#ifndef ec_key_fromdata_st
struct ec_key_fromdata_st {
    OSSL_PARAM *priv;
    OSSL_PARAM *pub;
};
#endif

#ifndef ec_key_fromdata_decoder
static ossl_unused int ec_key_fromdata_decoder
    (const OSSL_PARAM *p, struct ec_key_fromdata_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'p':
                switch(s[1]) {
                default:
                    break;
                case 'r':
                    if (ossl_likely(strcmp("iv", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_PRIV_KEY */
                        if (ossl_unlikely(r->priv != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->priv = (OSSL_PARAM *)p;
                    }
                    break;
                case 'u':
                    if (ossl_likely(strcmp("b", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_PUB_KEY */
                        if (ossl_unlikely(r->pub != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->pub = (OSSL_PARAM *)p;
                    }
                }
            }
    return 1;
}
#endif
/* End of machine generated */

#define ec_key_otherparams_fromdata_st ec_params_st
/* Machine generated */
#ifndef ec_key_otherparams_fromdata_list
static ossl_unused const OSSL_PARAM ec_key_otherparams_fromdata_list[] = {
    OSSL_PARAM_int(OSSL_PKEY_PARAM_USE_COFACTOR_ECDH, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC, NULL),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT, NULL, 0),
    OSSL_PARAM_END
};
#endif

#ifndef ec_key_otherparams_fromdata_st
struct ec_key_otherparams_fromdata_st {
    OSSL_PARAM *group_check;
    OSSL_PARAM *include_public;
    OSSL_PARAM *pt_format;
    OSSL_PARAM *use_cofactor;
};
#endif

#ifndef ec_key_otherparams_fromdata_decoder
static ossl_unused int ec_key_otherparams_fromdata_decoder
    (const OSSL_PARAM *p, struct ec_key_otherparams_fromdata_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'g':
                if (ossl_likely(strcmp("roup-check", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE */
                    if (ossl_unlikely(r->group_check != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->group_check = (OSSL_PARAM *)p;
                }
                break;
            case 'i':
                if (ossl_likely(strcmp("nclude-public", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC */
                    if (ossl_unlikely(r->include_public != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->include_public = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                if (ossl_likely(strcmp("oint-format", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT */
                    if (ossl_unlikely(r->pt_format != NULL)) {
                        break;
                    }
                    r->pt_format = (OSSL_PARAM *)p;
                }
                break;
            case 'u':
                if (ossl_likely(strcmp("se-cofactor-flag", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_USE_COFACTOR_ECDH */
                    if (ossl_unlikely(r->use_cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->use_cofactor = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */

#define ec_pkey_import_from_st ec_params_st
/* Machine generated */
#ifndef ec_pkey_import_from_list
static ossl_unused const OSSL_PARAM ec_pkey_import_from_list[] = {
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_GROUP_NAME, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_ENCODING, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_FIELD_TYPE, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_P, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_A, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_B, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_GENERATOR, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_ORDER, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_COFACTOR, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_SEED, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS, NULL),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_PUB_KEY, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_PRIV_KEY, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_USE_COFACTOR_ECDH, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC, NULL),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE, NULL, 0),
    OSSL_PARAM_END
};
#endif

#ifndef ec_pkey_import_from_st
struct ec_pkey_import_from_st {
    OSSL_PARAM *a;
    OSSL_PARAM *b;
    OSSL_PARAM *cofactor;
    OSSL_PARAM *decoded;
    OSSL_PARAM *encoding;
    OSSL_PARAM *field_type;
    OSSL_PARAM *generator;
    OSSL_PARAM *group_check;
    OSSL_PARAM *group_name;
    OSSL_PARAM *include_public;
    OSSL_PARAM *order;
    OSSL_PARAM *p;
    OSSL_PARAM *priv;
    OSSL_PARAM *pt_format;
    OSSL_PARAM *pub;
    OSSL_PARAM *seed;
    OSSL_PARAM *use_cofactor;
};
#endif

#ifndef ec_pkey_import_from_decoder
static ossl_unused int ec_pkey_import_from_decoder
    (const OSSL_PARAM *p, struct ec_pkey_import_from_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'a':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->a != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->a = (OSSL_PARAM *)p;
                }
                break;
            case 'b':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->b != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->b = (OSSL_PARAM *)p;
                }
                break;
            case 'c':
                if (ossl_likely(strcmp("ofactor", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_COFACTOR */
                    if (ossl_unlikely(r->cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->cofactor = (OSSL_PARAM *)p;
                }
                break;
            case 'd':
                if (ossl_likely(strcmp("ecoded-from-explicit", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS */
                    if (ossl_unlikely(r->decoded != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->decoded = (OSSL_PARAM *)p;
                }
                break;
            case 'e':
                if (ossl_likely(strcmp("ncoding", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ENCODING */
                    if (ossl_unlikely(r->encoding != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->encoding = (OSSL_PARAM *)p;
                }
                break;
            case 'f':
                if (ossl_likely(strcmp("ield-type", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_FIELD_TYPE */
                    if (ossl_unlikely(r->field_type != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->field_type = (OSSL_PARAM *)p;
                }
                break;
            case 'g':
                switch(s[1]) {
                default:
                    break;
                case 'e':
                    if (ossl_likely(strcmp("nerator", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_GENERATOR */
                        if (ossl_unlikely(r->generator != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->generator = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    switch(s[2]) {
                    default:
                        break;
                    case 'o':
                        switch(s[3]) {
                        default:
                            break;
                        case 'u':
                            switch(s[4]) {
                            default:
                                break;
                            case 'p':
                                switch(s[5]) {
                                default:
                                    break;
                                case '-':
                                    if (ossl_likely(strcmp("check", s + 6) == 0)) {
                                        /* OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE */
                                        if (ossl_unlikely(r->group_check != NULL)) {
                                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                           "param %s is repeated", s);
                                            return 0;
                                        }
                                        r->group_check = (OSSL_PARAM *)p;
                                    }
                                    break;
                                case '\0':
                                    if (ossl_unlikely(r->group_name != NULL)) {
                                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                       "param %s is repeated", s);
                                        return 0;
                                    }
                                    r->group_name = (OSSL_PARAM *)p;
                                }
                            }
                        }
                    }
                }
                break;
            case 'i':
                if (ossl_likely(strcmp("nclude-public", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC */
                    if (ossl_unlikely(r->include_public != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->include_public = (OSSL_PARAM *)p;
                }
                break;
            case 'o':
                if (ossl_likely(strcmp("rder", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ORDER */
                    if (ossl_unlikely(r->order != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->order = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                switch(s[1]) {
                default:
                    break;
                case 'o':
                    if (ossl_likely(strcmp("int-format", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT */
                        if (ossl_unlikely(r->pt_format != NULL)) {
                            break;
                        }
                        r->pt_format = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    if (ossl_likely(strcmp("iv", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_PRIV_KEY */
                        if (ossl_unlikely(r->priv != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->priv = (OSSL_PARAM *)p;
                    }
                    break;
                case 'u':
                    if (ossl_likely(strcmp("b", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_PUB_KEY */
                        if (ossl_unlikely(r->pub != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->pub = (OSSL_PARAM *)p;
                    }
                    break;
                case '\0':
                    if (ossl_unlikely(r->p != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->p = (OSSL_PARAM *)p;
                }
                break;
            case 's':
                if (ossl_likely(strcmp("eed", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_SEED */
                    if (ossl_unlikely(r->seed != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->seed = (OSSL_PARAM *)p;
                }
                break;
            case 'u':
                if (ossl_likely(strcmp("se-cofactor-flag", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_USE_COFACTOR_ECDH */
                    if (ossl_unlikely(r->use_cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->use_cofactor = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */

/* Keymgmt get and set parameter surfaces. */
#define ec_get_params_st ec_params_st
/* Machine generated */
#ifndef ec_get_params_list
static ossl_unused const OSSL_PARAM ec_get_params_list[] = {
    OSSL_PARAM_int(OSSL_PKEY_PARAM_BITS, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_FIELD_DEGREE, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_SECURITY_BITS, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_MAX_SIZE, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_SECURITY_CATEGORY, NULL),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_DEFAULT_DIGEST, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_ENCODED_PUBLIC_KEY, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_PUB_X, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_PUB_Y, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_GROUP_NAME, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_ENCODING, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_FIELD_TYPE, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_P, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_A, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_B, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_GENERATOR, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_ORDER, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_COFACTOR, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_SEED, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS, NULL),
# if !defined(OPENSSL_NO_EC2M)
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_CHAR2_M, NULL),
# endif
# if !defined(OPENSSL_NO_EC2M)
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_CHAR2_TYPE, NULL, 0),
# endif
# if !defined(OPENSSL_NO_EC2M)
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_CHAR2_TP_BASIS, NULL),
# endif
# if !defined(OPENSSL_NO_EC2M)
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_CHAR2_PP_K1, NULL),
# endif
# if !defined(OPENSSL_NO_EC2M)
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_CHAR2_PP_K2, NULL),
# endif
# if !defined(OPENSSL_NO_EC2M)
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_CHAR2_PP_K3, NULL),
# endif
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_PUB_KEY, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_PRIV_KEY, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_USE_COFACTOR_ECDH, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC, NULL),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE, NULL, 0),
    OSSL_PARAM_END
};
#endif

#ifndef ec_get_params_st
struct ec_get_params_st {
    OSSL_PARAM *a;
    OSSL_PARAM *b;
    OSSL_PARAM *bits;
# if !defined(OPENSSL_NO_EC2M)
    OSSL_PARAM *char2_k1;
# endif
# if !defined(OPENSSL_NO_EC2M)
    OSSL_PARAM *char2_k2;
# endif
# if !defined(OPENSSL_NO_EC2M)
    OSSL_PARAM *char2_k3;
# endif
# if !defined(OPENSSL_NO_EC2M)
    OSSL_PARAM *char2_m;
# endif
# if !defined(OPENSSL_NO_EC2M)
    OSSL_PARAM *char2_tp;
# endif
# if !defined(OPENSSL_NO_EC2M)
    OSSL_PARAM *char2_type;
# endif
    OSSL_PARAM *cofactor;
    OSSL_PARAM *decoded;
    OSSL_PARAM *default_digest;
    OSSL_PARAM *encoded_pub;
    OSSL_PARAM *encoding;
    OSSL_PARAM *field_degree;
    OSSL_PARAM *field_type;
    OSSL_PARAM *generator;
    OSSL_PARAM *group_check;
    OSSL_PARAM *group_name;
    OSSL_PARAM *include_public;
    OSSL_PARAM *maxsize;
    OSSL_PARAM *order;
    OSSL_PARAM *p;
    OSSL_PARAM *priv;
    OSSL_PARAM *pt_format;
    OSSL_PARAM *pub;
    OSSL_PARAM *pub_x;
    OSSL_PARAM *pub_y;
    OSSL_PARAM *secbits;
    OSSL_PARAM *seccat;
    OSSL_PARAM *seed;
    OSSL_PARAM *use_cofactor;
};
#endif

#ifndef ec_get_params_decoder
static ossl_unused int ec_get_params_decoder
    (const OSSL_PARAM *p, struct ec_get_params_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'a':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->a != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->a = (OSSL_PARAM *)p;
                }
                break;
            case 'b':
                switch(s[1]) {
                default:
                    break;
                case 'a':
# if !defined(OPENSSL_NO_EC2M)
                    if (ossl_likely(strcmp("sis-type", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_CHAR2_TYPE */
                        if (ossl_unlikely(r->char2_type != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->char2_type = (OSSL_PARAM *)p;
                    }
# endif
                    break;
                case 'i':
                    if (ossl_likely(strcmp("ts", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_BITS */
                        if (ossl_unlikely(r->bits != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->bits = (OSSL_PARAM *)p;
                    }
                    break;
                case '\0':
                    if (ossl_unlikely(r->b != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->b = (OSSL_PARAM *)p;
                }
                break;
            case 'c':
                if (ossl_likely(strcmp("ofactor", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_COFACTOR */
                    if (ossl_unlikely(r->cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->cofactor = (OSSL_PARAM *)p;
                }
                break;
            case 'd':
                switch(s[1]) {
                default:
                    break;
                case 'e':
                    switch(s[2]) {
                    default:
                        break;
                    case 'c':
                        if (ossl_likely(strcmp("oded-from-explicit", s + 3) == 0)) {
                            /* OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS */
                            if (ossl_unlikely(r->decoded != NULL)) {
                                ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                               "param %s is repeated", s);
                                return 0;
                            }
                            r->decoded = (OSSL_PARAM *)p;
                        }
                        break;
                    case 'f':
                        if (ossl_likely(strcmp("ault-digest", s + 3) == 0)) {
                            /* OSSL_PKEY_PARAM_DEFAULT_DIGEST */
                            if (ossl_unlikely(r->default_digest != NULL)) {
                                ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                               "param %s is repeated", s);
                                return 0;
                            }
                            r->default_digest = (OSSL_PARAM *)p;
                        }
                    }
                }
                break;
            case 'e':
                switch(s[1]) {
                default:
                    break;
                case 'n':
                    switch(s[2]) {
                    default:
                        break;
                    case 'c':
                        switch(s[3]) {
                        default:
                            break;
                        case 'o':
                            switch(s[4]) {
                            default:
                                break;
                            case 'd':
                                switch(s[5]) {
                                default:
                                    break;
                                case 'e':
                                    if (ossl_likely(strcmp("d-pub-key", s + 6) == 0)) {
                                        /* OSSL_PKEY_PARAM_ENCODED_PUBLIC_KEY */
                                        if (ossl_unlikely(r->encoded_pub != NULL)) {
                                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                           "param %s is repeated", s);
                                            return 0;
                                        }
                                        r->encoded_pub = (OSSL_PARAM *)p;
                                    }
                                    break;
                                case 'i':
                                    if (ossl_likely(strcmp("ng", s + 6) == 0)) {
                                        /* OSSL_PKEY_PARAM_EC_ENCODING */
                                        if (ossl_unlikely(r->encoding != NULL)) {
                                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                           "param %s is repeated", s);
                                            return 0;
                                        }
                                        r->encoding = (OSSL_PARAM *)p;
                                    }
                                }
                            }
                        }
                    }
                }
                break;
            case 'f':
                switch(s[1]) {
                default:
                    break;
                case 'i':
                    switch(s[2]) {
                    default:
                        break;
                    case 'e':
                        switch(s[3]) {
                        default:
                            break;
                        case 'l':
                            switch(s[4]) {
                            default:
                                break;
                            case 'd':
                                switch(s[5]) {
                                default:
                                    break;
                                case '-':
                                    switch(s[6]) {
                                    default:
                                        break;
                                    case 'd':
                                        if (ossl_likely(strcmp("egree", s + 7) == 0)) {
                                            /* OSSL_PKEY_PARAM_EC_FIELD_DEGREE */
                                            if (ossl_unlikely(r->field_degree != NULL)) {
                                                ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                               "param %s is repeated", s);
                                                return 0;
                                            }
                                            r->field_degree = (OSSL_PARAM *)p;
                                        }
                                        break;
                                    case 't':
                                        if (ossl_likely(strcmp("ype", s + 7) == 0)) {
                                            /* OSSL_PKEY_PARAM_EC_FIELD_TYPE */
                                            if (ossl_unlikely(r->field_type != NULL)) {
                                                ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                               "param %s is repeated", s);
                                                return 0;
                                            }
                                            r->field_type = (OSSL_PARAM *)p;
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
                break;
            case 'g':
                switch(s[1]) {
                default:
                    break;
                case 'e':
                    if (ossl_likely(strcmp("nerator", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_GENERATOR */
                        if (ossl_unlikely(r->generator != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->generator = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    switch(s[2]) {
                    default:
                        break;
                    case 'o':
                        switch(s[3]) {
                        default:
                            break;
                        case 'u':
                            switch(s[4]) {
                            default:
                                break;
                            case 'p':
                                switch(s[5]) {
                                default:
                                    break;
                                case '-':
                                    if (ossl_likely(strcmp("check", s + 6) == 0)) {
                                        /* OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE */
                                        if (ossl_unlikely(r->group_check != NULL)) {
                                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                           "param %s is repeated", s);
                                            return 0;
                                        }
                                        r->group_check = (OSSL_PARAM *)p;
                                    }
                                    break;
                                case '\0':
                                    if (ossl_unlikely(r->group_name != NULL)) {
                                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                       "param %s is repeated", s);
                                        return 0;
                                    }
                                    r->group_name = (OSSL_PARAM *)p;
                                }
                            }
                        }
                    }
                }
                break;
            case 'i':
                if (ossl_likely(strcmp("nclude-public", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC */
                    if (ossl_unlikely(r->include_public != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->include_public = (OSSL_PARAM *)p;
                }
                break;
            case 'k':
                switch(s[1]) {
                default:
                    break;
                case '1':
                    switch(s[2]) {
                    default:
                        break;
                    case '\0':
# if !defined(OPENSSL_NO_EC2M)
                        if (ossl_unlikely(r->char2_k1 != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->char2_k1 = (OSSL_PARAM *)p;
# endif
                        break;
                    }
                    break;
                case '2':
                    switch(s[2]) {
                    default:
                        break;
                    case '\0':
# if !defined(OPENSSL_NO_EC2M)
                        if (ossl_unlikely(r->char2_k2 != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->char2_k2 = (OSSL_PARAM *)p;
# endif
                        break;
                    }
                    break;
                case '3':
                    switch(s[2]) {
                    default:
                        break;
                    case '\0':
# if !defined(OPENSSL_NO_EC2M)
                        if (ossl_unlikely(r->char2_k3 != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->char2_k3 = (OSSL_PARAM *)p;
# endif
                        break;
                    }
                    break;
                }
                break;
            case 'm':
                switch(s[1]) {
                default:
                    break;
                case 'a':
                    if (ossl_likely(strcmp("x-size", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_MAX_SIZE */
                        if (ossl_unlikely(r->maxsize != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->maxsize = (OSSL_PARAM *)p;
                    }
                    break;
                case '\0':
# if !defined(OPENSSL_NO_EC2M)
                    if (ossl_unlikely(r->char2_m != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->char2_m = (OSSL_PARAM *)p;
# endif
                    break;
                }
                break;
            case 'o':
                if (ossl_likely(strcmp("rder", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ORDER */
                    if (ossl_unlikely(r->order != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->order = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                switch(s[1]) {
                default:
                    break;
                case 'o':
                    if (ossl_likely(strcmp("int-format", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT */
                        if (ossl_unlikely(r->pt_format != NULL)) {
                            break;
                        }
                        r->pt_format = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    if (ossl_likely(strcmp("iv", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_PRIV_KEY */
                        if (ossl_unlikely(r->priv != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->priv = (OSSL_PARAM *)p;
                    }
                    break;
                case 'u':
                    if (ossl_likely(strcmp("b", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_PUB_KEY */
                        if (ossl_unlikely(r->pub != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->pub = (OSSL_PARAM *)p;
                    }
                    break;
                case '\0':
                    if (ossl_unlikely(r->p != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->p = (OSSL_PARAM *)p;
                }
                break;
            case 'q':
                switch(s[1]) {
                default:
                    break;
                case 'x':
                    switch(s[2]) {
                    default:
                        break;
                    case '\0':
                        if (ossl_unlikely(r->pub_x != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->pub_x = (OSSL_PARAM *)p;
                    }
                    break;
                case 'y':
                    switch(s[2]) {
                    default:
                        break;
                    case '\0':
                        if (ossl_unlikely(r->pub_y != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->pub_y = (OSSL_PARAM *)p;
                    }
                }
                break;
            case 's':
                switch(s[1]) {
                default:
                    break;
                case 'e':
                    switch(s[2]) {
                    default:
                        break;
                    case 'c':
                        switch(s[3]) {
                        default:
                            break;
                        case 'u':
                            switch(s[4]) {
                            default:
                                break;
                            case 'r':
                                switch(s[5]) {
                                default:
                                    break;
                                case 'i':
                                    switch(s[6]) {
                                    default:
                                        break;
                                    case 't':
                                        switch(s[7]) {
                                        default:
                                            break;
                                        case 'y':
                                            switch(s[8]) {
                                            default:
                                                break;
                                            case '-':
                                                switch(s[9]) {
                                                default:
                                                    break;
                                                case 'b':
                                                    if (ossl_likely(strcmp("its", s + 10) == 0)) {
                                                        /* OSSL_PKEY_PARAM_SECURITY_BITS */
                                                        if (ossl_unlikely(r->secbits != NULL)) {
                                                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                                           "param %s is repeated", s);
                                                            return 0;
                                                        }
                                                        r->secbits = (OSSL_PARAM *)p;
                                                    }
                                                    break;
                                                case 'c':
                                                    if (ossl_likely(strcmp("ategory", s + 10) == 0)) {
                                                        /* OSSL_PKEY_PARAM_SECURITY_CATEGORY */
                                                        if (ossl_unlikely(r->seccat != NULL)) {
                                                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                                           "param %s is repeated", s);
                                                            return 0;
                                                        }
                                                        r->seccat = (OSSL_PARAM *)p;
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                        break;
                    case 'e':
                        if (ossl_likely(strcmp("d", s + 3) == 0)) {
                            /* OSSL_PKEY_PARAM_EC_SEED */
                            if (ossl_unlikely(r->seed != NULL)) {
                                ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                               "param %s is repeated", s);
                                return 0;
                            }
                            r->seed = (OSSL_PARAM *)p;
                        }
                    }
                }
                break;
            case 't':
# if !defined(OPENSSL_NO_EC2M)
                if (ossl_likely(strcmp("p", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_CHAR2_TP_BASIS */
                    if (ossl_unlikely(r->char2_tp != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->char2_tp = (OSSL_PARAM *)p;
                }
# endif
                break;
            case 'u':
                if (ossl_likely(strcmp("se-cofactor-flag", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_USE_COFACTOR_ECDH */
                    if (ossl_unlikely(r->use_cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->use_cofactor = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */

#define sm2_get_params_st ec_params_st
/* Machine generated */
#ifndef sm2_get_params_list
static ossl_unused const OSSL_PARAM sm2_get_params_list[] = {
    OSSL_PARAM_int(OSSL_PKEY_PARAM_BITS, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_FIELD_DEGREE, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_SECURITY_BITS, NULL),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_MAX_SIZE, NULL),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_DEFAULT_DIGEST, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_ENCODED_PUBLIC_KEY, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_GROUP_NAME, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_ENCODING, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_FIELD_TYPE, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_P, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_A, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_B, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_GENERATOR, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_ORDER, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_COFACTOR, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_SEED, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS, NULL),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_PUB_KEY, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_PRIV_KEY, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_PUB_X, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_PUB_Y, NULL, 0),
    OSSL_PARAM_END
};
#endif

#ifndef sm2_get_params_st
struct sm2_get_params_st {
    OSSL_PARAM *a;
    OSSL_PARAM *b;
    OSSL_PARAM *bits;
    OSSL_PARAM *cofactor;
    OSSL_PARAM *decoded;
    OSSL_PARAM *default_digest;
    OSSL_PARAM *encoded_pub;
    OSSL_PARAM *encoding;
    OSSL_PARAM *field_degree;
    OSSL_PARAM *field_type;
    OSSL_PARAM *generator;
    OSSL_PARAM *group_name;
    OSSL_PARAM *maxsize;
    OSSL_PARAM *order;
    OSSL_PARAM *p;
    OSSL_PARAM *priv;
    OSSL_PARAM *pt_format;
    OSSL_PARAM *pub;
    OSSL_PARAM *pub_x;
    OSSL_PARAM *pub_y;
    OSSL_PARAM *secbits;
    OSSL_PARAM *seed;
};
#endif

#ifndef sm2_get_params_decoder
static ossl_unused int sm2_get_params_decoder
    (const OSSL_PARAM *p, struct sm2_get_params_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'a':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->a != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->a = (OSSL_PARAM *)p;
                }
                break;
            case 'b':
                switch(s[1]) {
                default:
                    break;
                case 'i':
                    if (ossl_likely(strcmp("ts", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_BITS */
                        if (ossl_unlikely(r->bits != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->bits = (OSSL_PARAM *)p;
                    }
                    break;
                case '\0':
                    if (ossl_unlikely(r->b != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->b = (OSSL_PARAM *)p;
                }
                break;
            case 'c':
                if (ossl_likely(strcmp("ofactor", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_COFACTOR */
                    if (ossl_unlikely(r->cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->cofactor = (OSSL_PARAM *)p;
                }
                break;
            case 'd':
                switch(s[1]) {
                default:
                    break;
                case 'e':
                    switch(s[2]) {
                    default:
                        break;
                    case 'c':
                        if (ossl_likely(strcmp("oded-from-explicit", s + 3) == 0)) {
                            /* OSSL_PKEY_PARAM_EC_DECODED_FROM_EXPLICIT_PARAMS */
                            if (ossl_unlikely(r->decoded != NULL)) {
                                ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                               "param %s is repeated", s);
                                return 0;
                            }
                            r->decoded = (OSSL_PARAM *)p;
                        }
                        break;
                    case 'f':
                        if (ossl_likely(strcmp("ault-digest", s + 3) == 0)) {
                            /* OSSL_PKEY_PARAM_DEFAULT_DIGEST */
                            if (ossl_unlikely(r->default_digest != NULL)) {
                                ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                               "param %s is repeated", s);
                                return 0;
                            }
                            r->default_digest = (OSSL_PARAM *)p;
                        }
                    }
                }
                break;
            case 'e':
                switch(s[1]) {
                default:
                    break;
                case 'n':
                    switch(s[2]) {
                    default:
                        break;
                    case 'c':
                        switch(s[3]) {
                        default:
                            break;
                        case 'o':
                            switch(s[4]) {
                            default:
                                break;
                            case 'd':
                                switch(s[5]) {
                                default:
                                    break;
                                case 'e':
                                    if (ossl_likely(strcmp("d-pub-key", s + 6) == 0)) {
                                        /* OSSL_PKEY_PARAM_ENCODED_PUBLIC_KEY */
                                        if (ossl_unlikely(r->encoded_pub != NULL)) {
                                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                           "param %s is repeated", s);
                                            return 0;
                                        }
                                        r->encoded_pub = (OSSL_PARAM *)p;
                                    }
                                    break;
                                case 'i':
                                    if (ossl_likely(strcmp("ng", s + 6) == 0)) {
                                        /* OSSL_PKEY_PARAM_EC_ENCODING */
                                        if (ossl_unlikely(r->encoding != NULL)) {
                                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                           "param %s is repeated", s);
                                            return 0;
                                        }
                                        r->encoding = (OSSL_PARAM *)p;
                                    }
                                }
                            }
                        }
                    }
                }
                break;
            case 'f':
                switch(s[1]) {
                default:
                    break;
                case 'i':
                    switch(s[2]) {
                    default:
                        break;
                    case 'e':
                        switch(s[3]) {
                        default:
                            break;
                        case 'l':
                            switch(s[4]) {
                            default:
                                break;
                            case 'd':
                                switch(s[5]) {
                                default:
                                    break;
                                case '-':
                                    switch(s[6]) {
                                    default:
                                        break;
                                    case 'd':
                                        if (ossl_likely(strcmp("egree", s + 7) == 0)) {
                                            /* OSSL_PKEY_PARAM_EC_FIELD_DEGREE */
                                            if (ossl_unlikely(r->field_degree != NULL)) {
                                                ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                               "param %s is repeated", s);
                                                return 0;
                                            }
                                            r->field_degree = (OSSL_PARAM *)p;
                                        }
                                        break;
                                    case 't':
                                        if (ossl_likely(strcmp("ype", s + 7) == 0)) {
                                            /* OSSL_PKEY_PARAM_EC_FIELD_TYPE */
                                            if (ossl_unlikely(r->field_type != NULL)) {
                                                ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                               "param %s is repeated", s);
                                                return 0;
                                            }
                                            r->field_type = (OSSL_PARAM *)p;
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
                break;
            case 'g':
                switch(s[1]) {
                default:
                    break;
                case 'e':
                    if (ossl_likely(strcmp("nerator", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_GENERATOR */
                        if (ossl_unlikely(r->generator != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->generator = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    if (ossl_likely(strcmp("oup", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_GROUP_NAME */
                        if (ossl_unlikely(r->group_name != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->group_name = (OSSL_PARAM *)p;
                    }
                }
                break;
            case 'm':
                if (ossl_likely(strcmp("ax-size", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_MAX_SIZE */
                    if (ossl_unlikely(r->maxsize != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->maxsize = (OSSL_PARAM *)p;
                }
                break;
            case 'o':
                if (ossl_likely(strcmp("rder", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ORDER */
                    if (ossl_unlikely(r->order != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->order = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                switch(s[1]) {
                default:
                    break;
                case 'o':
                    if (ossl_likely(strcmp("int-format", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT */
                        if (ossl_unlikely(r->pt_format != NULL)) {
                            break;
                        }
                        r->pt_format = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    if (ossl_likely(strcmp("iv", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_PRIV_KEY */
                        if (ossl_unlikely(r->priv != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->priv = (OSSL_PARAM *)p;
                    }
                    break;
                case 'u':
                    if (ossl_likely(strcmp("b", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_PUB_KEY */
                        if (ossl_unlikely(r->pub != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->pub = (OSSL_PARAM *)p;
                    }
                    break;
                case '\0':
                    if (ossl_unlikely(r->p != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->p = (OSSL_PARAM *)p;
                }
                break;
            case 'q':
                switch(s[1]) {
                default:
                    break;
                case 'x':
                    switch(s[2]) {
                    default:
                        break;
                    case '\0':
                        if (ossl_unlikely(r->pub_x != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->pub_x = (OSSL_PARAM *)p;
                    }
                    break;
                case 'y':
                    switch(s[2]) {
                    default:
                        break;
                    case '\0':
                        if (ossl_unlikely(r->pub_y != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->pub_y = (OSSL_PARAM *)p;
                    }
                }
                break;
            case 's':
                switch(s[1]) {
                default:
                    break;
                case 'e':
                    switch(s[2]) {
                    default:
                        break;
                    case 'c':
                        if (ossl_likely(strcmp("urity-bits", s + 3) == 0)) {
                            /* OSSL_PKEY_PARAM_SECURITY_BITS */
                            if (ossl_unlikely(r->secbits != NULL)) {
                                ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                               "param %s is repeated", s);
                                return 0;
                            }
                            r->secbits = (OSSL_PARAM *)p;
                        }
                        break;
                    case 'e':
                        if (ossl_likely(strcmp("d", s + 3) == 0)) {
                            /* OSSL_PKEY_PARAM_EC_SEED */
                            if (ossl_unlikely(r->seed != NULL)) {
                                ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                               "param %s is repeated", s);
                                return 0;
                            }
                            r->seed = (OSSL_PARAM *)p;
                        }
                    }
                }
            }
    return 1;
}
#endif
/* End of machine generated */

#define ec_set_params_st ec_params_st
/* Machine generated */
#ifndef ec_set_params_list
static ossl_unused const OSSL_PARAM ec_set_params_list[] = {
    OSSL_PARAM_int(OSSL_PKEY_PARAM_USE_COFACTOR_ECDH, NULL),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_ENCODED_PUBLIC_KEY, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_ENCODING, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_SEED, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC, NULL),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE, NULL, 0),
    OSSL_PARAM_END
};
#endif

#ifndef ec_set_params_st
struct ec_set_params_st {
    OSSL_PARAM *encoded_pub;
    OSSL_PARAM *encoding;
    OSSL_PARAM *group_check;
    OSSL_PARAM *include_public;
    OSSL_PARAM *pt_format;
    OSSL_PARAM *seed;
    OSSL_PARAM *use_cofactor;
};
#endif

#ifndef ec_set_params_decoder
static ossl_unused int ec_set_params_decoder
    (const OSSL_PARAM *p, struct ec_set_params_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'e':
                switch(s[1]) {
                default:
                    break;
                case 'n':
                    switch(s[2]) {
                    default:
                        break;
                    case 'c':
                        switch(s[3]) {
                        default:
                            break;
                        case 'o':
                            switch(s[4]) {
                            default:
                                break;
                            case 'd':
                                switch(s[5]) {
                                default:
                                    break;
                                case 'e':
                                    if (ossl_likely(strcmp("d-pub-key", s + 6) == 0)) {
                                        /* OSSL_PKEY_PARAM_ENCODED_PUBLIC_KEY */
                                        if (ossl_unlikely(r->encoded_pub != NULL)) {
                                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                           "param %s is repeated", s);
                                            return 0;
                                        }
                                        r->encoded_pub = (OSSL_PARAM *)p;
                                    }
                                    break;
                                case 'i':
                                    if (ossl_likely(strcmp("ng", s + 6) == 0)) {
                                        /* OSSL_PKEY_PARAM_EC_ENCODING */
                                        if (ossl_unlikely(r->encoding != NULL)) {
                                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                           "param %s is repeated", s);
                                            return 0;
                                        }
                                        r->encoding = (OSSL_PARAM *)p;
                                    }
                                }
                            }
                        }
                    }
                }
                break;
            case 'g':
                if (ossl_likely(strcmp("roup-check", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE */
                    if (ossl_unlikely(r->group_check != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->group_check = (OSSL_PARAM *)p;
                }
                break;
            case 'i':
                if (ossl_likely(strcmp("nclude-public", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC */
                    if (ossl_unlikely(r->include_public != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->include_public = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                if (ossl_likely(strcmp("oint-format", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT */
                    if (ossl_unlikely(r->pt_format != NULL)) {
                        break;
                    }
                    r->pt_format = (OSSL_PARAM *)p;
                }
                break;
            case 's':
                if (ossl_likely(strcmp("eed", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_SEED */
                    if (ossl_unlikely(r->seed != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->seed = (OSSL_PARAM *)p;
                }
                break;
            case 'u':
                if (ossl_likely(strcmp("se-cofactor-flag", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_USE_COFACTOR_ECDH */
                    if (ossl_unlikely(r->use_cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->use_cofactor = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */

#define sm2_set_params_st ec_params_st
/* Machine generated */
#ifndef sm2_set_params_list
static ossl_unused const OSSL_PARAM sm2_set_params_list[] = {
    OSSL_PARAM_int(OSSL_PKEY_PARAM_USE_COFACTOR_ECDH, NULL),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_ENCODED_PUBLIC_KEY, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_ENCODING, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_SEED, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC, NULL),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE, NULL, 0),
    OSSL_PARAM_END
};
#endif

#ifndef sm2_set_params_st
struct sm2_set_params_st {
    OSSL_PARAM *encoded_pub;
    OSSL_PARAM *encoding;
    OSSL_PARAM *group_check;
    OSSL_PARAM *include_public;
    OSSL_PARAM *pt_format;
    OSSL_PARAM *seed;
    OSSL_PARAM *use_cofactor;
};
#endif

#ifndef sm2_set_params_decoder
static ossl_unused int sm2_set_params_decoder
    (const OSSL_PARAM *p, struct sm2_set_params_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'e':
                switch(s[1]) {
                default:
                    break;
                case 'n':
                    switch(s[2]) {
                    default:
                        break;
                    case 'c':
                        switch(s[3]) {
                        default:
                            break;
                        case 'o':
                            switch(s[4]) {
                            default:
                                break;
                            case 'd':
                                switch(s[5]) {
                                default:
                                    break;
                                case 'e':
                                    if (ossl_likely(strcmp("d-pub-key", s + 6) == 0)) {
                                        /* OSSL_PKEY_PARAM_ENCODED_PUBLIC_KEY */
                                        if (ossl_unlikely(r->encoded_pub != NULL)) {
                                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                           "param %s is repeated", s);
                                            return 0;
                                        }
                                        r->encoded_pub = (OSSL_PARAM *)p;
                                    }
                                    break;
                                case 'i':
                                    if (ossl_likely(strcmp("ng", s + 6) == 0)) {
                                        /* OSSL_PKEY_PARAM_EC_ENCODING */
                                        if (ossl_unlikely(r->encoding != NULL)) {
                                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                           "param %s is repeated", s);
                                            return 0;
                                        }
                                        r->encoding = (OSSL_PARAM *)p;
                                    }
                                }
                            }
                        }
                    }
                }
                break;
            case 'g':
                if (ossl_likely(strcmp("roup-check", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE */
                    if (ossl_unlikely(r->group_check != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->group_check = (OSSL_PARAM *)p;
                }
                break;
            case 'i':
                if (ossl_likely(strcmp("nclude-public", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_INCLUDE_PUBLIC */
                    if (ossl_unlikely(r->include_public != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->include_public = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                if (ossl_likely(strcmp("oint-format", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT */
                    if (ossl_unlikely(r->pt_format != NULL)) {
                        break;
                    }
                    r->pt_format = (OSSL_PARAM *)p;
                }
                break;
            case 's':
                if (ossl_likely(strcmp("eed", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_SEED */
                    if (ossl_unlikely(r->seed != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->seed = (OSSL_PARAM *)p;
                }
                break;
            case 'u':
                if (ossl_likely(strcmp("se-cofactor-flag", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_USE_COFACTOR_ECDH */
                    if (ossl_unlikely(r->use_cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->use_cofactor = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */

/* Key generation context parameters. */
#define ec_gen_set_params_st ec_params_st
/* Machine generated */
#ifndef ec_gen_set_params_list
static ossl_unused const OSSL_PARAM ec_gen_set_params_list[] = {
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_GROUP_NAME, NULL, 0),
    OSSL_PARAM_int(OSSL_PKEY_PARAM_USE_COFACTOR_ECDH, NULL),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_ENCODING, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_FIELD_TYPE, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_P, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_A, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_B, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_GENERATOR, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_ORDER, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_COFACTOR, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_SEED, NULL, 0),
# if !defined(FIPS_MODULE)
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_DHKEM_IKM, NULL, 0),
# endif
# if defined(FIPS_MODULE)
    OSSL_PARAM_int(OSSL_PKEY_PARAM_FIPS_KEY_CHECK, NULL),
# endif
    OSSL_PARAM_END
};
#endif

#ifndef ec_gen_set_params_st
struct ec_gen_set_params_st {
    OSSL_PARAM *a;
    OSSL_PARAM *b;
    OSSL_PARAM *cofactor;
# if !defined(FIPS_MODULE)
    OSSL_PARAM *dhkem_ikm;
# endif
    OSSL_PARAM *encoding;
    OSSL_PARAM *field_type;
# if defined(FIPS_MODULE)
    OSSL_PARAM *fips_key_check;
# endif
    OSSL_PARAM *generator;
    OSSL_PARAM *group_check;
    OSSL_PARAM *group_name;
    OSSL_PARAM *order;
    OSSL_PARAM *p;
    OSSL_PARAM *pt_format;
    OSSL_PARAM *seed;
    OSSL_PARAM *use_cofactor;
};
#endif

#ifndef ec_gen_set_params_decoder
static ossl_unused int ec_gen_set_params_decoder
    (const OSSL_PARAM *p, struct ec_gen_set_params_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'a':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->a != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->a = (OSSL_PARAM *)p;
                }
                break;
            case 'b':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->b != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->b = (OSSL_PARAM *)p;
                }
                break;
            case 'c':
                if (ossl_likely(strcmp("ofactor", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_COFACTOR */
                    if (ossl_unlikely(r->cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->cofactor = (OSSL_PARAM *)p;
                }
                break;
            case 'd':
# if !defined(FIPS_MODULE)
                if (ossl_likely(strcmp("hkem-ikm", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_DHKEM_IKM */
                    if (ossl_unlikely(r->dhkem_ikm != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->dhkem_ikm = (OSSL_PARAM *)p;
                }
# endif
                break;
            case 'e':
                if (ossl_likely(strcmp("ncoding", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ENCODING */
                    if (ossl_unlikely(r->encoding != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->encoding = (OSSL_PARAM *)p;
                }
                break;
            case 'f':
                if (ossl_likely(strcmp("ield-type", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_FIELD_TYPE */
                    if (ossl_unlikely(r->field_type != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->field_type = (OSSL_PARAM *)p;
                }
                break;
            case 'g':
                switch(s[1]) {
                default:
                    break;
                case 'e':
                    if (ossl_likely(strcmp("nerator", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_GENERATOR */
                        if (ossl_unlikely(r->generator != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->generator = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    switch(s[2]) {
                    default:
                        break;
                    case 'o':
                        switch(s[3]) {
                        default:
                            break;
                        case 'u':
                            switch(s[4]) {
                            default:
                                break;
                            case 'p':
                                switch(s[5]) {
                                default:
                                    break;
                                case '-':
                                    if (ossl_likely(strcmp("check", s + 6) == 0)) {
                                        /* OSSL_PKEY_PARAM_EC_GROUP_CHECK_TYPE */
                                        if (ossl_unlikely(r->group_check != NULL)) {
                                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                           "param %s is repeated", s);
                                            return 0;
                                        }
                                        r->group_check = (OSSL_PARAM *)p;
                                    }
                                    break;
                                case '\0':
                                    if (ossl_unlikely(r->group_name != NULL)) {
                                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                       "param %s is repeated", s);
                                        return 0;
                                    }
                                    r->group_name = (OSSL_PARAM *)p;
                                }
                            }
                        }
                    }
                }
                break;
            case 'k':
# if defined(FIPS_MODULE)
                if (ossl_likely(strcmp("ey-check", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_FIPS_KEY_CHECK */
                    if (ossl_unlikely(r->fips_key_check != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->fips_key_check = (OSSL_PARAM *)p;
                }
# endif
                break;
            case 'o':
                if (ossl_likely(strcmp("rder", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ORDER */
                    if (ossl_unlikely(r->order != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->order = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                switch(s[1]) {
                default:
                    break;
                case 'o':
                    if (ossl_likely(strcmp("int-format", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT */
                        if (ossl_unlikely(r->pt_format != NULL)) {
                            break;
                        }
                        r->pt_format = (OSSL_PARAM *)p;
                    }
                    break;
                case '\0':
                    if (ossl_unlikely(r->p != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->p = (OSSL_PARAM *)p;
                }
                break;
            case 's':
                if (ossl_likely(strcmp("eed", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_SEED */
                    if (ossl_unlikely(r->seed != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->seed = (OSSL_PARAM *)p;
                }
                break;
            case 'u':
                if (ossl_likely(strcmp("se-cofactor-flag", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_USE_COFACTOR_ECDH */
                    if (ossl_unlikely(r->use_cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->use_cofactor = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */

#define sm2_gen_set_params_st ec_params_st
/* Machine generated */
#ifndef sm2_gen_set_params_list
static ossl_unused const OSSL_PARAM sm2_gen_set_params_list[] = {
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_GROUP_NAME, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_ENCODING, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_PKEY_PARAM_EC_FIELD_TYPE, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_P, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_A, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_B, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_GENERATOR, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_ORDER, NULL, 0),
    OSSL_PARAM_BN(OSSL_PKEY_PARAM_EC_COFACTOR, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_PKEY_PARAM_EC_SEED, NULL, 0),
    OSSL_PARAM_END
};
#endif

#ifndef sm2_gen_set_params_st
struct sm2_gen_set_params_st {
    OSSL_PARAM *a;
    OSSL_PARAM *b;
    OSSL_PARAM *cofactor;
    OSSL_PARAM *encoding;
    OSSL_PARAM *field_type;
    OSSL_PARAM *generator;
    OSSL_PARAM *group_name;
    OSSL_PARAM *order;
    OSSL_PARAM *p;
    OSSL_PARAM *pt_format;
    OSSL_PARAM *seed;
};
#endif

#ifndef sm2_gen_set_params_decoder
static ossl_unused int sm2_gen_set_params_decoder
    (const OSSL_PARAM *p, struct sm2_gen_set_params_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'a':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->a != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->a = (OSSL_PARAM *)p;
                }
                break;
            case 'b':
                switch(s[1]) {
                default:
                    break;
                case '\0':
                    if (ossl_unlikely(r->b != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->b = (OSSL_PARAM *)p;
                }
                break;
            case 'c':
                if (ossl_likely(strcmp("ofactor", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_COFACTOR */
                    if (ossl_unlikely(r->cofactor != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->cofactor = (OSSL_PARAM *)p;
                }
                break;
            case 'e':
                if (ossl_likely(strcmp("ncoding", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ENCODING */
                    if (ossl_unlikely(r->encoding != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->encoding = (OSSL_PARAM *)p;
                }
                break;
            case 'f':
                if (ossl_likely(strcmp("ield-type", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_FIELD_TYPE */
                    if (ossl_unlikely(r->field_type != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->field_type = (OSSL_PARAM *)p;
                }
                break;
            case 'g':
                switch(s[1]) {
                default:
                    break;
                case 'e':
                    if (ossl_likely(strcmp("nerator", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_GENERATOR */
                        if (ossl_unlikely(r->generator != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->generator = (OSSL_PARAM *)p;
                    }
                    break;
                case 'r':
                    if (ossl_likely(strcmp("oup", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_GROUP_NAME */
                        if (ossl_unlikely(r->group_name != NULL)) {
                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                           "param %s is repeated", s);
                            return 0;
                        }
                        r->group_name = (OSSL_PARAM *)p;
                    }
                }
                break;
            case 'o':
                if (ossl_likely(strcmp("rder", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_ORDER */
                    if (ossl_unlikely(r->order != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->order = (OSSL_PARAM *)p;
                }
                break;
            case 'p':
                switch(s[1]) {
                default:
                    break;
                case 'o':
                    if (ossl_likely(strcmp("int-format", s + 2) == 0)) {
                        /* OSSL_PKEY_PARAM_EC_POINT_CONVERSION_FORMAT */
                        if (ossl_unlikely(r->pt_format != NULL)) {
                            break;
                        }
                        r->pt_format = (OSSL_PARAM *)p;
                    }
                    break;
                case '\0':
                    if (ossl_unlikely(r->p != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->p = (OSSL_PARAM *)p;
                }
                break;
            case 's':
                if (ossl_likely(strcmp("eed", s + 1) == 0)) {
                    /* OSSL_PKEY_PARAM_EC_SEED */
                    if (ossl_unlikely(r->seed != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->seed = (OSSL_PARAM *)p;
                }
            }
    return 1;
}
#endif
/* End of machine generated */

#define ec_gen_get_params_st ec_params_st
/* Machine generated */
#ifndef ec_gen_get_params_list
static ossl_unused const OSSL_PARAM ec_gen_get_params_list[] = {
# if defined(FIPS_MODULE)
    OSSL_PARAM_int(OSSL_PKEY_PARAM_FIPS_APPROVED_INDICATOR, NULL),
# endif
    OSSL_PARAM_END
};
#endif

#ifndef ec_gen_get_params_st
struct ec_gen_get_params_st {
# if defined(FIPS_MODULE)
    OSSL_PARAM *fips_indicator;
# else
    int dummy; /* unused */
# endif
};
#endif

#ifndef ec_gen_get_params_decoder
static ossl_unused int ec_gen_get_params_decoder
    (const OSSL_PARAM *p, struct ec_gen_get_params_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
# if defined(FIPS_MODULE)
            if (ossl_likely(strcmp("fips-indicator", s + 0) == 0)) {
                /* OSSL_PKEY_PARAM_FIPS_APPROVED_INDICATOR */
                if (ossl_unlikely(r->fips_indicator != NULL)) {
                    ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                   "param %s is repeated", s);
                    return 0;
                }
                r->fips_indicator = (OSSL_PARAM *)p;
            }
# else
            ;
# endif
    return 1;
}
#endif
/* End of machine generated */

/* clang-format on */

#endif
