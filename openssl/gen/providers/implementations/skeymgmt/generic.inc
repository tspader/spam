/*
 * Copyright 2025-2026 The OpenSSL Project Authors. All Rights Reserved.
 *
 * Licensed under the Apache License 2.0 (the "License").  You may not use
 * this file except in compliance with the License.  You can obtain a copy
 * in the file LICENSE in the source distribution or at
 * https://www.openssl.org/source/license.html
 */


/* Machine generated by util/perl/OpenSSL/paramnames.pm */
#include <string.h>
#include <openssl/params.h>
#include <openssl/e_os2.h>
#include "internal/common.h"
#include "prov/proverr.h"

/* Machine generated */
#ifndef generic_skey_import_list
static ossl_unused const OSSL_PARAM generic_skey_import_list[] = {
    OSSL_PARAM_octet_string(OSSL_SKEY_PARAM_RAW_BYTES, NULL, 0),
    OSSL_PARAM_utf8_string(OSSL_SKEY_PARAM_ALIAS, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_SKEY_PARAM_LOCAL_KEYID, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_SKEY_PARAM_ALGORITHM_OID, NULL, 0),
    OSSL_PARAM_octet_string(OSSL_SKEY_PARAM_ALGORITHM_PARAMS, NULL, 0),
    OSSL_PARAM_END
};
#endif

#ifndef generic_skey_import_st
struct generic_skey_import_st {
    OSSL_PARAM *algorithm_oid;
    OSSL_PARAM *algorithm_params;
    OSSL_PARAM *alias;
    OSSL_PARAM *local_keyid;
    OSSL_PARAM *raw_bytes;
};
#endif

#ifndef generic_skey_import_decoder
static ossl_unused int generic_skey_import_decoder
    (const OSSL_PARAM *p, struct generic_skey_import_st *r)
{
    const char *s;

    memset(r, 0, sizeof(*r));
    if (p != NULL)
        for (; (s = p->key) != NULL; p++)
            switch(s[0]) {
            default:
                break;
            case 'r':
                if (ossl_likely(strcmp("aw-bytes", s + 1) == 0)) {
                    /* OSSL_SKEY_PARAM_RAW_BYTES */
                    if (ossl_unlikely(r->raw_bytes != NULL)) {
                        ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                       "param %s is repeated", s);
                        return 0;
                    }
                    r->raw_bytes = (OSSL_PARAM *)p;
                }
                break;
            case 's':
                switch(s[1]) {
                default:
                    break;
                case 'k':
                    switch(s[2]) {
                    default:
                        break;
                    case 'e':
                        switch(s[3]) {
                        default:
                            break;
                        case 'y':
                            switch(s[4]) {
                            default:
                                break;
                            case '-':
                                switch(s[5]) {
                                default:
                                    break;
                                case 'a':
                                    switch(s[6]) {
                                    default:
                                        break;
                                    case 'l':
                                        switch(s[7]) {
                                        default:
                                            break;
                                        case 'g':
                                            switch(s[8]) {
                                            default:
                                                break;
                                            case 'o':
                                                switch(s[9]) {
                                                default:
                                                    break;
                                                case 'r':
                                                    switch(s[10]) {
                                                    default:
                                                        break;
                                                    case 'i':
                                                        switch(s[11]) {
                                                        default:
                                                            break;
                                                        case 't':
                                                            switch(s[12]) {
                                                            default:
                                                                break;
                                                            case 'h':
                                                                switch(s[13]) {
                                                                default:
                                                                    break;
                                                                case 'm':
                                                                    switch(s[14]) {
                                                                    default:
                                                                        break;
                                                                    case '-':
                                                                        switch(s[15]) {
                                                                        default:
                                                                            break;
                                                                        case 'o':
                                                                            if (ossl_likely(strcmp("id", s + 16) == 0)) {
                                                                                /* OSSL_SKEY_PARAM_ALGORITHM_OID */
                                                                                if (ossl_unlikely(r->algorithm_oid != NULL)) {
                                                                                    ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                                                                   "param %s is repeated", s);
                                                                                    return 0;
                                                                                }
                                                                                r->algorithm_oid = (OSSL_PARAM *)p;
                                                                            }
                                                                            break;
                                                                        case 'p':
                                                                            if (ossl_likely(strcmp("arams", s + 16) == 0)) {
                                                                                /* OSSL_SKEY_PARAM_ALGORITHM_PARAMS */
                                                                                if (ossl_unlikely(r->algorithm_params != NULL)) {
                                                                                    ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                                                                   "param %s is repeated", s);
                                                                                    return 0;
                                                                                }
                                                                                r->algorithm_params = (OSSL_PARAM *)p;
                                                                            }
                                                                        }
                                                                    }
                                                                }
                                                            }
                                                        }
                                                    }
                                                }
                                            }
                                            break;
                                        case 'i':
                                            if (ossl_likely(strcmp("as", s + 8) == 0)) {
                                                /* OSSL_SKEY_PARAM_ALIAS */
                                                if (ossl_unlikely(r->alias != NULL)) {
                                                    ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                                   "param %s is repeated", s);
                                                    return 0;
                                                }
                                                r->alias = (OSSL_PARAM *)p;
                                            }
                                        }
                                    }
                                    break;
                                case 'l':
                                    if (ossl_likely(strcmp("ocal-keyid", s + 6) == 0)) {
                                        /* OSSL_SKEY_PARAM_LOCAL_KEYID */
                                        if (ossl_unlikely(r->local_keyid != NULL)) {
                                            ERR_raise_data(ERR_LIB_PROV, PROV_R_REPEATED_PARAMETER,
                                                           "param %s is repeated", s);
                                            return 0;
                                        }
                                        r->local_keyid = (OSSL_PARAM *)p;
                                    }
                                }
                            }
                        }
                    }
                }
            }
    return 1;
}
#endif
/* End of machine generated */
